---
id: LOG-6b0f39d7a4c1
type: log
slug: jwt-verify-removed
title: jwt.verify removed from session.ts
created: 2026-07-26T14:02:00Z
author: claude-code/1.4.2
scope:
  - src/auth/**
about: TASK-8f3a91c2d4e7
schema: 3
version: 1
---

Anything the line cannot hold.

The instant is `created`, the identity is `author`, the message is `title`, and
`about` names the entity the entry is about — the four things the rendered line
prints, stored as fields. No field is added for the message: every entity
already carries the one a lister prints.

There is no `status`, and there is nothing missing: an entry is written once and
has nothing to transition to. `version` stays, and an entry whose `version` is
above 1 has been rewritten — which is what the field is worth on a kind written
once.
