- 2026-08-13T21:58:40Z claude-code/ca7b — Measured the current behaviour on a fixture corpus holding one entity at schema 4 and one at schema 1. show on the ahead entity already names found and supported, because store loads the file and the parse error surfaces. Every listing verb answers as if the file did not exist: find, context and graph print only the readable entity, and status prints '1 fault(s)' without naming the cause. check is the only verb that names it, and only among its findings. So the missing half is exactly the one the task states: the message never reaches a reader running an ordinary verb. The index cannot carry the answer either: refresh counts an unparseable file as unreadable, then remembers its hash, so the second invocation reports nothing at all.
- 2026-08-13T22:12:37Z claude-code/ca7b — cargo test --workspace is not green on this machine, and it is not this task's doing: a_shallow_clone_cannot_explain_a_dead_scope_and_says_so_instead_of_faulting fails identically on a clean tree at origin/main (2215a78), verified by stashing this work and running that test alone. clone_of builds file:///C:/Users/... and git 2.54.0.windows.1 reads the path back as /C:/Users/..., so the clone never happens. Everything else passes: 150 of 151 in tests/cli.rs, plus ank-core and the skill suite.
- 2026-08-13T22:14:28Z claude-code/ca7b — done, proof commit:20b1bd1
