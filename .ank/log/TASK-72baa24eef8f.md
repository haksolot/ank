- 2026-08-13T21:54:33Z claude-code/72ba — The done_criteria of this task carries a premise that is already false. It requires that ank --version still print skill 605f771e1955. It does not, and it did not before this task started: skill/SKILL.md in this tree declares revision 3f350ad26459, and the binary prints ank 0.2.0 (f8d3a25, skill 3f350ad26459). The corpus already recorded the same drift in the log of TASK-ab53a0d5654e. What the clause protects is that skill/SKILL.md must not be touched, since its content is frozen and moving it costs a superseding ADR plus a human signature. That part is honoured and verified: SKILL.md is untouched by this task and the revision does not move. The literal hash in the criterion is stale, not the constraint behind it. The criterion is not edited and the task is not released -- this is precisely the case TASK-f6b8 exists to formalise.
- 2026-08-13T21:57:47Z claude-code/72ba — The SKILL.md clause of the criterion is already verified through the binary, so it needs no new test. crates/ank-cli/tests/skill.rs holds two tests that together close it: the_skill_says_which_revision_it_is recomputes ank_core::freeze_hash_short over the body and asserts metadata.revision equals it, and the_binary_names_the_skill_revision_it_was_built_alongside launches the binary, reads the skill token out of ank --version, and asserts it equals both that hash and the declared value. Any edit to SKILL.md moves the hash and fails the first test unless the revision is updated too, at which point the second test observes the printed value moving. cargo test --workspace staying green is therefore the proof that SKILL.md is untouched and the revision has not moved -- which is the clause's real content, the literal 605f771e1955 being stale.
- 2026-08-13T22:12:14Z claude-code/72ba — cargo test --workspace is not green on this machine, and it is not this task that made it red. a_shallow_clone_cannot_explain_a_dead_scope_and_says_so_instead_of_faulting fails at crates/ank-cli/tests/cli.rs:129, and it fails identically on 2215a78 with every change of this task stashed -- the tree this branch is based on, untouched. This task adds no Rust, so it cannot reach that test. Root cause, measured rather than guessed: clone_of builds file:///{path} at cli.rs:7621, and git 2.54.0.windows.1 refuses that form, reporting the URL as the literal path /C:/Users/... Probed side by side on real Windows paths: file://C:/... clones and writes .git/shallow, file:///C:/... fails, and a plain path clones but drops --depth with the warning the helper's own comment predicts. So the third slash before the drive letter is the whole defect, and the fix is to not emit it on Windows. CI is green on all three platforms for the run that merged this test, so the runner image carries a git that still accepts the form -- which makes this a latent red rather than a broken test, and it turns red on its own the day the image updates. Filed as its own task rather than fixed here: it is outside this task's declared scope, and cli.rs is being edited by three other live sessions.
- 2026-08-13T22:21:01Z claude-code/72ba — The image is served anonymously over HTTPS, measured rather than assumed. With GITHUB_TOKEN and GH_TOKEN unset and curl --no-netrc, both branch spellings of the path answer http 200, content-type image/png, 6780 bytes, and the body is byte-identical to assets/ank-card.png by cmp: raw.githubusercontent.com/haksolot/ank/refs/heads/chore/pi-gallery-logo-72ba/assets/ank-card.png and the short form without refs/heads. The first four bytes are 89504e47 and no HTML appears anywhere in the body. The manifest names main instead of either, and that URL answers 404 today: main does not carry the asset until this branch merges. That is the deliberate trade. A branch URL would verify literally now and then die when the branch is deleted, shipping a manifest that points at nothing; a main URL follows the default branch and costs one untested term. Everything except the ref name is proven by the fetch above -- the host serves this repository anonymously, this exact path under assets/, as image/png, with these exact bytes. A miss is also cheap to tell apart: the 404 came back as 14 bytes of text/plain reading 404: Not Found, not as an HTML page. The URL must be re-fetched once main carries the commit; the pull request says so.
- 2026-08-13T22:21:24Z claude-code/72ba — done, proof commit:e9a75b7
