- 2026-08-15T08:48:48Z claude-code/spec-kind-cli — surveyed the CLI surfaces the kind has to reach: new subcommands (cli.rs CommandSpec + commands.rs runtime match, mandatory_flags, flag_form, skeleton, template guidance, create_filled, supersedes_of), find --type and its two counters, the two-bucket partition in scope, short_ids walking a fixed array, and context which has no spec section at all. show already falls through correctly: its per-kind arms are Task-with-wildcard, so a spec prints frontmatter and body and gets no task's edges or proofs.
- 2026-08-15T09:09:34Z claude-code/spec-kind-cli — falsified every clause of the new integration test one at a time: injecting a body line into spec_section, into the execution page and into the JSON specs array each fires the negative assertion in its own mode; removing the orientation and execution spec sections, disabling the find --type filter, trimming show's output and reordering new's subcommands each fires its positive assertion. All reverted, test green.
