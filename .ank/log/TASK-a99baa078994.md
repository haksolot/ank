- 2026-07-31T18:06:56Z claude-code@ank — FTS5 table carrying id, title, slug and criteria - the text a scan opened every file for. Maintained by upsert and forget inside the same transaction as the entity rows, so the two cannot disagree. Ranking is bm25 with documented positional weights, id above title above slug above criterion, and the identifier breaks ties so the order is total. User text never reaches FTS5 as syntax: every term is quoted and suffixed with a star, which is why OR, NOT and a stray quote come back as no match instead of an error. Two things found rather than assumed: there is no fts5 feature on rusqlite 0.40 because bundled already compiles SQLITE_ENABLE_FTS5 in, and the search is now prefix rather than substring.
- 2026-07-31T18:07:48Z claude-code@ank — done, proof test:local/06a29375ed3b@3f99cde
- 2026-07-31T18:15Z claude-code@ank — run 30653970821 green on the three OS, appended as ci:// because the FTS5 table is built by the SQLite compiled into each platform binary and a table that exists on Windows proves nothing about musl. verify was absent from this task and has been added: with no verifier declared, done would have written a proof anchoring nothing, which is the one shape of proof worse than none.
