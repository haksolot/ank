- 2026-08-14T18:30:36Z claude-code/072c — Reused git::file_at as the body asks, but on the layout directory rather than per entity: cat-file -p <rev>:.ank/entities prints the tree, and a tree already carries the blob hash of every file in it. One file_at per entity would have been 400+ processes on this 203-entity corpus. The working-tree side is batched git hash-object, which is already allowed plumbing (section 12) and, unlike a byte comparison of the two texts, applies the same CRLF conversion the commit did -- read literally, a Windows checkout with core.autocrlf on would differ from the default branch in every single file and elsewhere in none. Measured on this corpus: check goes from 32.5s to 35.0s warm.
- 2026-08-14T18:30:48Z claude-code/072c — A repository with no commit at all is a third state, and it has to be silent: file_at cannot tell 'branch names nothing because nothing was ever committed' from 'default_branch is mistyped', since both are an unresolvable revision. HEAD resolving is what separates them -- no commit anywhere is the nominal state of a fresh ank init, a mistyped branch in a repository full of history is not. Without that split the drift signal would fire on every fixture in the suite that never commits.
