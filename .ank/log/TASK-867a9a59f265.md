- 2026-08-15T10:10:48Z claude-code/spec-ratified — accept, amend, check and edit reach a spec through one Anchored view, so the walk, the hash and the signature stay one mechanism; only the hashed text and the commit key differ (body+scope, never constraint+scope over a kind that declares none)
- 2026-08-15T10:10:57Z claude-code/spec-ratified — left outside this criterion, on purpose: review and status still filter the ratification queue on EntityKind::Adr, so a proposed spec waits for a human nowhere a human looks. accept draws from that queue and now promotes specs, so the gap is real; it is not one of the three verbs the criterion names, and widening review's live section (which counts what a constraint binds, and a spec binds nothing) is a decision of its own
