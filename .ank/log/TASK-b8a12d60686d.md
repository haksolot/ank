- 2026-08-11T02:52:01Z seanl@sean-laptop — Direction settled with the maintainer: --repo is refused by name on init. The deciding fact is in repo.rs, not in taste -- repo::at requires the given path to contain a .ank/, and init's target by definition does not carry one yet. Accepting the flag here would give it a second meaning, an existing repository everywhere else and a directory to create on this verb, which is the hazard section 4 already invokes against a -s meaning --status in find and --scope in new. The positional ank init <path> already says where, so the refusal costs a round trip and loses no expressiveness. What matters for the defect either way: init never writes into the current repository while a --repo naming another was given.
- 2026-08-11T02:59:35Z seanl@sean-laptop — Section 4 and section 9 first, then the code. One consequence I had not expected: section 9 already carries the rule this fix runs into -- a flag that is always refused is not a flag, it belongs in the refusals and not in the flag list -- and being global is not an exemption, so ank help init offering --repo was the same defect in the reading surface. CommandSpec gains refuses_globals, read by the per-verb page and by --json alike, because a name hidden from one rendering and not the others is that defect in a quieter place. The flat listing keeps stating the three globals unqualified: the exception belongs on the page of the verb that makes it, and qualifying it there would be a second structure in an output ADR-c656 keeps flat. The integration test runs the binary from inside one repository with --repo naming another, which is the shape of the defect: a test from a neutral directory would have passed against the old behaviour.
- 2026-08-11T03:03:30Z seanl@sean-laptop — done, proof test:31454073701
