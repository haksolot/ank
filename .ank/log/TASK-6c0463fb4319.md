- 2026-08-15T07:09:39Z claude-code/merge-union — Verified by experiment, not by reading: in a scratch repo with no merge=union, two branches each appending one line to the same log file conflict on merge (exit 1, conflict markers in the file). The same repo with .ank/log/** merge=union merges clean and keeps both lines. The criterion's premise holds; the three texts are wrong.
- 2026-08-15T07:33:32Z claude-code/merge-union — show caps its log and never its entity: sect 4 guarantees the entity byte for byte, so a cut there is a wrong answer rather than a short one. The entity is charged first and in full, the log gets what is left, and the cut names ank log <id> -- the same budget with no entity to pay for, so strictly more room. log itself names ank config context_budget <n>, the exact number at which nothing would be cut.
- 2026-08-15T07:33:39Z claude-code/merge-union — Falsified all three, each restored after: removing .ank/log/** merge=union makes the merge test fail with a real CONFLICT and markers in the file; disabling the log-directory walk makes check exit 0 on a log full of markers, which is exactly the silent pass the task describes; unbounding the room prints all twelve entries and the cap test fails.
