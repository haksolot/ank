---
id: TASK-c75e2a43e712
type: task
slug: a-prerelease-tag-publishes-as-latest-and-nothing
title: A prerelease tag publishes as latest, and nothing decided that
created: 2026-08-08T18:21:15Z
author: seanl@sean-laptop
status: done
scope:
  - .github/workflows/release.yml
  - docs/ank-spec-v1.1.md
  - .github/scripts/npm-dist-tag.sh
blocked_by: []
done_criteria: |
  A tag matching v* whose version is a prerelease does not reach the registry under
  the latest dist-tag. What it does instead -- refuse, or publish under a
  prerelease tag -- is decided in the specification or an ADR first, then written
  once and passed by both npm-smoke and publish-npm, which keep passing the same
  flags as each other. Demonstrated on a dispatch, not on a real tag.
  cargo test and ank check stay green.
criteria_by: creator
proof:
  - type: test
    ref: "31297086914"
    criteria: 5ca121d235ae
  - type: test
    ref: "31297079005"
    criteria: 5ca121d235ae
schema: 2
version: 7
---

Recorded while fixing TASK-ace80749fd5c, and deliberately not decided there.

release.yml fires on push of any tag matching v*, which includes v0.2.0-rc1. The
version reaching npm-assemble.sh is ${GITHUB_REF_NAME#v}, so such a tag publishes
0.2.0-rc1 to the registry. Under npm 10 that landed on the latest dist-tag
without a word, which means every user running npm install -g @haksolot/ank would
have got a release candidate. The fix for ace8 makes that explicit rather than
implicit -- publish now passes --tag latest -- so the behaviour is unchanged and
now visible, which is the point at which it becomes worth a decision.

The options are not equivalent and the choice is not obvious. Refusing the tag
outright keeps the registry clean and costs the project the ability to ship a
candidate at all. Deriving the dist-tag from the version, latest for a release
and next for a prerelease, is the npm convention and means a candidate is
installable by anyone who asks for it by name without touching what a bare
install resolves to. A third answer is to narrow the workflow trigger so a
prerelease tag builds and publishes a GitHub release without reaching npm.

Whichever is chosen, the constraint that ace8 established holds: the smoke job
and the publish job pass the same flags, or the rehearsal stops rehearsing. That
is what made this worth separating rather than folding in -- deciding it inside
ace8 would have changed release behaviour under cover of a compatibility fix.

No prerelease tag has ever been pushed here, so nothing is broken today.

## Log
- 2026-08-09T05:32:50Z seanl@sean-laptop — amended: +scope docs/ank-spec-v1.1.md, +scope .github/scripts/npm-dist-tag.sh
- 2026-08-09T05:37:07Z seanl@sean-laptop — Decision recorded in section 9 rather than as an ADR, and the reason is procedural rather than a preference: an ADR lands proposed, binds nobody, and needs a human signature through accept on the default branch -- so minting one would have blocked this task on the maintainer. The criterion allows either, and the specification is the source of truth in any case.

Chose the derived dist-tag of the three options the task laid out. It is the only one that keeps the ability to ship a candidate at all, and it is derived from the version rather than chosen at tag time because a flag someone has to remember while pushing a tag is a flag that is eventually forgotten -- and this one fails silently, which is the whole finding.

Wrote the derivation as one script both jobs read. The smoke job assembles a single version, so its dry-run only ever exercises one branch of the rule; that is why there is a separate table step asserting both directions before anything publishes. The branch a dry-run cannot reach is precisely the one that would have put a release candidate on every bare install.

Two things measured rather than assumed. Build metadata with a hyphen in it, 1.2.3+build-7, would have been read as a prerelease by a naive search for a hyphen -- stripping metadata first is in the script and in the table. And the GitHub release job had no checkout at all, so marking a prerelease there meant either a second copy of the rule or one more step; it got the checkout, because two channels reading one tag differently is how a reader ends up trusting whichever they happened to open.
- 2026-08-09T05:43:07Z seanl@sean-laptop — Demonstrated on the dispatch, run 31297086914: all seven mappings assert on all three platforms, and the derived tag reaches the dry-run publish (dist-tag: next for 0.0.0-smoke). publish and publish-npm skipped, which is the gate on refs/tags/v doing its job -- a dispatch proves the pipeline without spending a tag.

Falsified the table assertion locally rather than spending a second fifteen-minute dispatch on it: the same check() with a deliberately wrong expectation exits 1 under set -e, and so does a mismatched comparison. The step can fail, which is the only thing that makes it worth having.

Note for whoever reads the run: the smoke job's own version is 0.0.0-smoke, a prerelease, so its dry-run only ever exercises the next branch. That is exactly why the table step exists and runs before it. The latest branch is unreachable from the dry-run alone, and it is the branch that would have put a release candidate on every bare install.
- 2026-08-09T05:43:07Z seanl@sean-laptop — done, proof test:31297086914
- 2026-08-09T05:43:17Z seanl@sean-laptop — attested test:31297079005
