---
id: TASK-1e6f3e97507c
type: task
slug: amending-a-plan-that-already-exists-has-no-comma
title: Amending a plan that already exists has no command
created: 2026-08-01T03:14:31Z
author: seanl@sean-laptop
status: done
scope:
  - crates/ank-cli/src/human.rs
  - crates/ank-cli/src/cli.rs
  - crates/ank-cli/tests/cli.rs
  - docs/ank-spec-v1.1.md
blocked_by: []
done_criteria: |
  A human-side verb amends the two fields of an existing entity that no command can reach today: blocked_by and scope. It adds and removes explicitly rather than replacing wholesale, resolves every reference at the point of the edit so an unknown one is refused there, increments version, and writes a log line naming what changed. It refuses to touch a frozen field, done_criteria above all. Amending the scope of a task under a live claim is allowed and warns, because the claim record anchors the hash of the constraints that scope selects. The agent surface stays at eight verbs. A test invokes the binary and reads the resulting file.
criteria_by: creator
verify: [cargo-test, fmt-check, check-repo]
proof:
  - type: test
    ref: local/316dc46a6687@3aa25b9
    tree: scope/4568886eb46e
    criteria: dadff3984c1a
    verifier: cargo-test@f14aeab36e1b
  - type: test
    ref: local/e3b0c44298fc@3aa25b9
    tree: scope/4568886eb46e
    criteria: dadff3984c1a
    verifier: fmt-check@5ca6d10bcd55
  - type: test
    ref: local/f722b459fdee@3aa25b9
    tree: scope/4568886eb46e
    criteria: dadff3984c1a
    verifier: check-repo@5734e9cf9d3d
schema: 2
version: 4
---

Two edits by hand this session, both in .ank/, both because no command exists.

TASK-3109a736c255 needed a blocked_by added after it was filed. TASK-84238e3e179d needed its scope corrected before it could be claimed at all -- ten struct literals across six files stop compiling the moment a field is added to Task or Adr, and the scope named neither them nor commands.rs, where the criterion's own 'ank new writes it' lives.

Both were legitimate. Neither was checkable by anything, which is the same argument TASK-1f4f7b57039b made for attest: an edit performed by hand is indistinguishable, in the resulting file, from any other edit performed by hand. A verb can guarantee that the fields it did not name come back byte-identical. A human with an editor cannot.

**It became urgent rather than merely untidy.** ADR-01b6dd05f0db closed .ank/ to direct reads and writes by an agent, and the PreToolUse hook that enforces it is checked in. It did not fire this session only because Claude Code loads hooks at startup and the file was born mid-session. From the next session on, an agent that needs to add a blocked_by is refused by the hook and has no command to fall back on -- the gap stops being a rough edge and becomes a dead end.

**Human surface, and the reasoning is TASK-bc214fd815b2's, already written down.** Adding a blocker to a task that already exists is a different act from creating one: it is a change to a plan that is not yours. ADR-3859eb46bdc3 freezes the agent surface at eight and sends new functionality here, and human.rs can grow freely, which is what makes that freeze sustainable.

**Add and remove, never replace.** A verb taking the whole list is a verb that silently drops what the caller forgot to repeat, and the round-trip guarantees nothing about intent. Naming what enters and what leaves is also what makes the log line worth reading afterwards.

**done_criteria is not on the list and must be refused loudly.** It is frozen by hash at claim, the anchor is held where the file's editor cannot reach it (ADR-6b3f19e08a24), and a verb that edited it would be offering to do the one thing the whole freeze exists to make visible. The refusal names ank release --reason instead, which is what a wrong criterion actually calls for.

**Scope under a live claim is the interesting case.** The claim record anchors the hash of the constraints applicable to the task's scope, so widening or narrowing that scope can change which ADRs bind the work in progress -- and done already warns when that hash has moved. Refusing the edit would be wrong: a scope discovered to be false mid-task is exactly the situation that produced this task. Allowing it silently would be worse. It is allowed and it warns, naming the holder.

## Log
- 2026-08-01T03:23:55Z seanl@sean-laptop — amend, sur la surface humaine. Add and remove explicitly rather than a replacement list: a verb given the whole list silently drops whatever the caller forgot to repeat, and the round-trip guarantees nothing about intent -- naming what enters and what leaves is also what makes the log line readable afterwards.

--criteria is declared on the verb purely so it can be refused by name. The parser's unknown-flag path would list the valid flags and leave the caller to work out why the obvious one is missing; section 4 wants the exact command to run next, and for a frozen criterion that command is ank release --reason. A 6 rather than a 7, because it is the freeze speaking and not a missing prerequisite.

Three refusals the criterion did not spell out but the corpus requires. A done or closed task: section 3 allows exactly one write after completion and it is attest's, so amending one would manufacture the fault check reports as a done task modified beyond appending a proof. An accepted ADR's scope: constraint and scope are hashed into the ratification commit, so the edit would diverge from the anchor and suspend the ADR's injection into context -- the hint names --supersedes, because changing a ratified decision is a succession. And a scope emptied out, for the reason new refuses one at creation: attachment happens through scope and nothing else.

Dropping something that is not there is refused rather than ignored, same doctrine as --verify on an ADR: succeeding quietly teaches the caller it worked. An amendment that changes nothing is a 7 too -- it would otherwise bump version and write a log line saying nothing happened.

The live-claim warning is tested with its negative half, without which it would pass against an implementation that warns on every amendment. Two test mistakes worth recording, both mine and neither the code's: 'aaaa' is not a leading prefix of 00000000aaaa, and an .unwrap_or on the call would have made the no-warning assertion pass vacuously.
- 2026-08-01T03:24:21Z seanl@sean-laptop — done, proof test:local/316dc46a6687@3aa25b9 test:local/e3b0c44298fc@3aa25b9 test:local/f722b459fdee@3aa25b9
