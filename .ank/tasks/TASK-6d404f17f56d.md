---
id: TASK-6d404f17f56d
type: task
slug: attest-detached-records-a-proof-in-a-ref-and-eve
title: attest --detached records a proof in a ref, and every reader unions the two
created: 2026-08-11T22:24:46Z
author: claude-code@sean-laptop
status: in_progress
scope:
  - crates/ank-cli/src/human.rs
  - crates/ank-cli/src/claim.rs
  - crates/ank-cli/src/git.rs
  - crates/ank-cli/src/context.rs
  - crates/ank-cli/tests/cli.rs
  - crates/ank-cli/src/cli.rs
blocked_by: []
done_criteria: |
  ank attest <id> --proof <type>:<ref> --detached writes a proof record to
  refs/ank/proof/<id>, pushes it when a remote exists, and leaves the working tree
  byte-identical -- asserted by comparing the task file before and after, and by
  git status reporting nothing.
  
  ank show <id> displays a detached proof alongside any proof in the file, marked
  as detached. ank check counts detached proofs when deciding whether a done task
  has a test proof, so the signal does not fire on a task anchored only by a ref.
  
  A detached proof is pruned by check once an equivalent proof appears in the task
  file on the default branch, and at no other time -- in particular no TTL prunes
  it.
  
  Asserted through the binary in crates/ank-cli/tests/cli.rs, across two clones of
  a bare origin: clone B attests, clone A fetches and shows the proof, and no
  commit exists in either.
criteria_by: creator
schema: 2
version: 4
---

Implements ADR-493471d64ba0. Read it before starting: the category being added is
external attestation, durable but authored by no branch, and the reason it goes
in a ref rather than a file is that it has no tree to travel in.

Almost nothing here is new machinery. The record is a blob written the way a claim
record is written; the push is the same `--force-with-lease` compare-and-swap; the
refspec `init` installs already fetches `refs/ank/*`, so an existing repository
needs no configuration change. Reuse all of it rather than writing a parallel
path, and rename what currently says "claim" in those helpers so the shared code
does not lie about what it carries.

Read through the one coordination enumeration in `context.rs` that every listing
verb already shares. A second walk over the refs would be free to disagree with
the first, which is the failure this codebase avoids by construction elsewhere.

The union must be symmetric. Do not treat the file as authoritative and the ref as
a cache: between a `done` landing on a branch and its merge, the ref is the only
place the reference exists, and that window is most of a branch's life. A reader
preferring the file would show an unanchored task and emit a signal telling
somebody to attest work that is already attested.

Mark the origin in the display. A detached proof lives outside the tree and a
reader deciding how much weight to give it needs to know that without being told
to prefer either.

The pruning predicate is the one `done_on` already implements for completion
refs. Reuse it. A proof ref pruned on time would delete the record precisely
during the window it exists to cover.

## Log
- 2026-08-13T05:46:58Z claude-agent-b — Implemented. The shared ref helpers now take a ref name instead of a task id and stopped saying claim: git::push_claim/fetch_claim became push_ref/fetch_ref, and claim.rs grew read_at/put_at/delete_at/sync_ref_from_remote with read/put/delete kept as claim-facing wrappers -- those do carry claims, so they do not lie. Record gained a Proof variant carrying a list of AttestedProof, each wrapping ank_core::Proof verbatim so the two sources cannot drift into two shapes of one thing; the list is what keeps append-only true on the ref side, since one address is the task. Both enumerations now walk the two namespaces once each rather than gaining a second walk: context::plane returns claims and proofs together with coordination() as a wrapper, and check's own coordination() does the same for the Record map it needs. A record whose state contradicts its namespace is a finding, never coerced -- read as the other kind it would present a held task as free or lose an attestation. The union in check is a single Vec over both sources feeding the no-proof fault, the weak-proof signal and the no-test-proof signal, which is the ADR's 'a signal that counts proofs counts both'. show prints a PROOFS section only when something is detached, and then lists both halves marked: the frontmatter above already carries the file's list byte for byte, so printing it twice on every done task would be noise, while the union exists only when a ref carries something. Pruning reuses the default-branch predicate and deletes only when every attestation in the record has an equivalent in the file, so a record holding an unmerged run survives. Two guards in the suite fired as designed and were answered rather than muted: --detached had to be classified in NOT_A_PATH, and listed_flags had to learn that a switch takes no value -- it was pushing a value at every flag, which for a switch would have measured a stray positional. Falsified by blinding both readers: all three binary tests fail. Filed TASK-ae77a9ee2964 for the specification half, which is outside this scope.
- 2026-08-13T05:47:20Z claude-agent-b — amended: +scope crates/ank-cli/src/cli.rs
