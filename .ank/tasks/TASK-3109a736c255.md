---
id: TASK-3109a736c255
type: task
slug: the-ank-directory-stops-being-read-and-written-b
title: The .ank directory stops being read and written by hand
created: 2026-07-31T22:33:35Z
status: done
scope:
  - skill/SKILL.md
  - .ank/adr/**
  - CLAUDE.md
  - .claude/**
blocked_by: [TASK-bc214fd815b2, TASK-1f4f7b57039b, TASK-85557f7ac5e7]
done_criteria: |
  Entities are read and written through the CLI only, and the instruction says so instead of the opposite. SKILL.md no longer tells the reader that cat is their show; it names ank show, ank find and ank context as the way in. A proposed ADR scoped to .ank/** carries the constraint, so ank context serves it to every agent at the top of every session. CLAUDE.md drops the maintained by hand clause, which is no longer true. A PreToolUse hook refuses Read, Write, Edit, Glob and Grep under .ank/ with a message naming the command to run instead, and it is checked into the repository so it travels with it rather than living on one machine.
criteria_by: creator
verify: [cargo-test, fmt-check, check-repo]
proof:
  - type: test
    ref: local/242a2fc2c0b8@47ddf47
    tree: scope/13129ed3013a
    criteria: 8ef2a17d8656
    verifier: cargo-test@f14aeab36e1b
  - type: test
    ref: local/e3b0c44298fc@47ddf47
    tree: scope/13129ed3013a
    criteria: 8ef2a17d8656
    verifier: fmt-check@5ca6d10bcd55
  - type: test
    ref: local/1982fe2d8bfa@47ddf47
    tree: scope/13129ed3013a
    criteria: 8ef2a17d8656
    verifier: check-repo@5734e9cf9d3d
schema: 1
version: 8
---

`.ank/` should be as opaque to an agent as `.git/` is. Nobody opens
`.git/refs/heads/main` to read a branch; they run `git rev-parse`. The reason is
not secrecy, it is that the tool knows things the file does not: the budget of
§5, the truncation notice, the freeze, what is claimable and by whom.

**The instruction currently says the opposite, in as many words.** SKILL.md:

    Read the entity file when you want the whole body. The format is the
    specification and `cat` is your `show`; the CLI is not the only way in.

An agent that follows the file it was given reads `.ank/` directly, and is
right to. That line goes first, or everything else here argues with it.

It was defensible when written and is not any more. `ank show <prefix>` returns
the entity byte for byte, frontmatter and body, for tasks and ADRs alike;
`ank find --status open` lists without needing a query; `ank context` serves the
constraints in full and says so when it truncates. Measured on 2026-07-31: not
one read of an entity requires opening a file. The write side was the real gap,
which is why this task is blocked on the two that close it rather than filed
alone.

The claim in CLAUDE.md that `.ank/` is "maintained by hand for as long as the
CLI cannot do it" is the same sentence read from the other end. Once the CLI
can, the clause has to go, or it stays a standing permission nobody revisits.

Three layers, and they are not redundant. The ADR states the rule where
`ank context` will serve it to every agent at the top of every session -- the
mechanism this project built for exactly this. SKILL.md stops contradicting it.
The hook is what actually refuses, because a constraint an agent can read is a
constraint an agent can forget, and §5 was never going to be enforced by good
intentions.

**The hook does not make ank a gatekeeper.** ADR-6b3f19e08a24 says no frozen
field may rely on the CLI refusing a write, and nothing here changes that: the
freezes stay anchored by hash and stay verifiable by anyone. What is being
constrained is the agent's harness, not the tool. A human with an editor keeps
every power they had; `check` remains the thing that notices.

Proposed, not accepted. §8 and the `roles` block of `config.yml` both say an
agent may write `new:adr:proposed` and never `adr:accept` -- the acceptance is
the human's, and TASK-313165e55d81 is what makes it mean something.

## Released once, and why

Claimed on 2026-07-31 and handed straight back. The criterion asks SKILL.md to
name `ank show`, and `crates/ank-cli/tests/skill.rs` asserts the opposite --
`show` is on the human surface, that test is out of this scope, and `cargo-test`
is a declared verifier here. Unsatisfiable as written.

The conflict was not the wording. With `cat` forbidden and `show` human, an
agent has no way left to read a task body at all: `context` serves the criterion
and the constraints, never the body, and the bodies are where the traps live.
The line "cat is your show" was the *consequence* of `show` being human, not an
oversight.

Resolved by decision rather than by weakening the criterion, which is now
satisfiable exactly as it stands: TASK-85557f7ac5e7 moves `show` onto the agent
surface, superseding ADR-2f8a61c04b7d, and this task is blocked on it.

## Log
- 2026-07-31T22:54:59Z seanl@sean-laptop — released: The criterion requires SKILL.md to name 'ank show', and tests/skill.rs asserts the opposite -- show is on the human surface, and that test is out of scope while cargo-test is a declared verifier here. The conflict is not the wording but the design underneath it: with cat forbidden and show human, an agent has no way left to read a task body, and context serves the criterion and the constraints but never the body. The line 'cat is your show' is the consequence of show being human, not an oversight. Releasing rather than weakening the criterion; the resolution is a decision about the agent surface and belongs to a human.
- 2026-08-01T02:03:30Z seanl@sean-laptop — Three layers, and the hook is the one that actually refuses. Node rather than a shell script: one file, a real JSON parser, and identical behaviour on all three platforms -- a sh version would have string-matched the payload without jq, and PowerShell is not a given off Windows. The matcher is deliberately narrow: a repo-wide Grep with no path is allowed through, because it merely might touch .ank/ and a hook that blocks the whole repository over a maybe is a hook people switch off. Absolute paths are resolved against CLAUDE_PROJECT_DIR so a .. cannot walk back in unnoticed. Exercised against twelve cases before shipping, seven refusals and five allowances, including Bash -- which is not in the matcher, so cat .ank/config.yml still passes; the hook constrains the tool surface an agent reaches for by default, not every conceivable route, and pretending otherwise would be the gatekeeper design ADR-6b3f rejects.

Found while verifying the ADR reaches agents: applicable_constraints serves accepted ADRs only, and that is load-bearing rather than an oversight -- a proposal binds nobody, so including it would move the constraints hash on a decision nobody has taken. Orientation mode is the one that lists proposals, by title, under PROPOSED (non-binding), which is where an agent with no claim lands at the top of every session. That is what the criterion asks for, and the full constraint text arrives in CONSTRAINTS when a human runs accept -- TASK-313165e55d81.

CLAUDE.md needed more than the maintained-by-hand clause: its working loop still instructed hand edits step by step (move the task to in_progress, increment version, write status: done and a proof entry), which argued with the ADR exactly as the SKILL.md line did. Rewritten as the seven-verb loop it actually is. Its surface bullet said 7 verbs, now 8 per ADR-3859.
- 2026-08-01T02:04:07Z seanl@sean-laptop — done, proof test:local/242a2fc2c0b8@47ddf47 test:local/e3b0c44298fc@47ddf47 test:local/1982fe2d8bfa@47ddf47
