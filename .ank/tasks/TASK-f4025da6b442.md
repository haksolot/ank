---
id: TASK-f4025da6b442
type: task
slug: a-task-in-progress-with-no-claim-ref-is-invisibl
title: A task in_progress with no claim ref is invisible to check
created: 2026-07-31T21:48:29Z
status: done
scope:
  - crates/ank-cli/src/human.rs
  - crates/ank-cli/tests/cli.rs
blocked_by: []
done_criteria: |
  check reports a signal for every task whose status is in_progress while no ref exists at refs/ank/claims/<id>, naming the task and the command that returns it to the pool. The signal is a signal and not a fault: a lapsed claim is a stale record, not a corrupt corpus, and it must never fail CI. A test builds that exact state -- an in_progress task with no ref -- and asserts the finding through the binary, because the ref namespace is the thing being read.
criteria_by: creator
verify: [cargo-test, fmt-check, check-repo]
proof:
  - type: test
    ref: local/e1724167b627@a1b873e
    tree: scope/ea85ba71c758
    criteria: "440840732736"
    verifier: cargo-test@f14aeab36e1b
  - type: test
    ref: local/e3b0c44298fc@a1b873e
    tree: scope/ea85ba71c758
    criteria: "440840732736"
    verifier: fmt-check@5ca6d10bcd55
  - type: test
    ref: local/7cffd961a830@a1b873e
    tree: scope/ea85ba71c758
    criteria: "440840732736"
    verifier: check-repo@5734e9cf9d3d
schema: 1
version: 5
---

TASK-daf25ab8a9b7 has been `in_progress` since commit 98dbf3b with no ref under
`refs/ank/claims/`. `context` lists it as live work, so nobody picks it up, and
nothing anywhere says it is not held. Fifteen signals on this corpus and not one
of them is about the task that is lying.

`check` already knows how to notice the neighbouring case: a claim present but
expired signals "claim by X expired: the task is claimable again"
(`human.rs`, in the `Record::Claim` arm). The gap is the arm that does not
exist — a status saying held with no record to hold it. The two are the same
invariant read from opposite sides, and only one of them is implemented.

Signal, never fault. A lapsed claim is a stale record and the corpus is intact;
failing CI over it would teach the reader that `check` exits 8 for reasons that
do not matter, which is the fastest way to make exit 8 mean nothing.

The scope covers `check` and the integration test rather than a fix to
TASK-daf25ab8a9b7 itself. Releasing that task by hand corrects today and
nothing else; the missing checker is the defect.

## Log
- 2026-07-31T22:18:46Z seanl@sean-laptop — The complement of an arm that already existed: check noticed a claim present but expired, and never a status saying held with no record at all. Same invariant from opposite sides, one of them unimplemented. Condition is !coord.contains_key, which is the criterion read literally -- both claim and completion records live at refs/ank/claims/<id>. Signal, never fault: the corpus is intact and only the record is stale, and an exit 8 that fires for things that do not matter is an exit code people learn to ignore. Tested through the binary because the ref namespace is what is being read, and the state is built the way it arises -- a real claim, then update-ref -d -- rather than by hand-writing status: in_progress, which would test a corpus nobody produces. Demonstrated on this repo: TASK-daf25ab8a9b7 is now named, and TASK-f4025da6b442 is not, because it holds its ref.
- 2026-07-31T22:19:09Z seanl@sean-laptop — done, proof test:local/e1724167b627@a1b873e test:local/e3b0c44298fc@a1b873e test:local/7cffd961a830@a1b873e
