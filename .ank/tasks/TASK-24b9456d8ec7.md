---
id: TASK-24b9456d8ec7
type: task
slug: accept-performs-the-succession-model-rs-says-it
title: accept performs the succession model.rs says it performs
created: 2026-07-31T22:59:46Z
status: done
scope:
  - crates/ank-cli/src/human.rs
blocked_by: [TASK-195538e3e15c]
done_criteria: |
  Accepting an ADR that names a supersedes target marks that target superseded, in the same operation and before the ratification commit, so the commit carries a corpus check leaves clean. A target that is not accepted is refused with the reason and the next command, and nothing is written. A target already superseded by another ADR is refused rather than silently re-pointed. Tests cover the succession, the refusal, and the fact that check reports no broken chain afterwards.
criteria_by: creator
verify: [cargo-test, fmt-check, check-repo]
proof:
  - type: test
    ref: local/e1cb39fbd427@47ddf47
    tree: scope/588eaa9c62c9
    criteria: 8909ec66d0b3
    verifier: cargo-test@f14aeab36e1b
  - type: test
    ref: local/e3b0c44298fc@47ddf47
    tree: scope/588eaa9c62c9
    criteria: 8909ec66d0b3
    verifier: fmt-check@5ca6d10bcd55
  - type: test
    ref: local/1001a6a2f058@47ddf47
    tree: scope/588eaa9c62c9
    criteria: 8909ec66d0b3
    verifier: check-repo@5734e9cf9d3d
schema: 1
version: 4
---

model.rs states it as settled fact:

    Accepted -> Superseded is the only legal write on an accepted ADR,
    performed by the accept of the ADR that replaces it.

accept does not do it. It promotes the proposed ADR, writes ratified, commits signed, and never touches the target. The transition the model declares legal has no code behind it, so the only way to mark an ADR superseded is to open the file -- which is the practice this whole line of work exists to end.

Two writes in one operation, and the order matters for the same reason it matters in done: durable state before the commit. The ratification commit is authoritative as soon as it exists, so it must not exist over a corpus that check would call broken.

Refusing a target that is not accepted is not pedantry. Superseding a proposal is meaningless -- nothing was ever binding -- and the caller almost certainly meant a different identifier.

## Log
- 2026-08-01T01:51:10Z seanl@sean-laptop — accept now performs the succession, and turned out not to be performing the acceptance either. check_transition(Accepted) was called and the assignment that follows it never written, so accept wrote ratified onto an ADR still marked proposed. Both tests that existed asserted refusals and returned before the commit, so nothing ever ran the path -- the exact shape CLAUDE.md warns about, and the fix needed a test that signs for real. Fixture signs with SSH rather than GPG: ssh-keygen ships beside git on all three platforms and needs no agent, keyring or passphrase prompt. Everything that can refuse refuses before anything is written, because a half-performed succession is a corpus the ratification commit would then make authoritative and accept has no second pass. Target written first, then the superseder: between the two writes the corpus holds a target marked superseded whose superseder is still proposed, which check_adr calls clean in both directions; the reverse order leaves an accepted superseder over an unmarked target, which is precisely the fault. One commit for both paths -- two would leave a window in which history says both constraints bind. A target that is not accepted is a 7 and not the 6 of an illegal transition: nothing transitions, a prerequisite is unmet, and the hint names ank accept on the target when it is merely proposed.
- 2026-08-01T01:51:41Z seanl@sean-laptop — done, proof test:local/e1cb39fbd427@47ddf47 test:local/e3b0c44298fc@47ddf47 test:local/1001a6a2f058@47ddf47
