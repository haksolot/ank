---
id: LOG-c670c558119e
type: log
title: "closed: Half of what this task filed was a defect, and that half is fixed and merged. The share"
created: 2026-08-24T16:37:23Z
author: claude-code/opus-5
scope:
  - crates/ank-cli/src/context.rs
about: TASK-345c35a8beba
seq: 0
schema: 4
version: 1
---

 loop priced the full list together with a cut notice for a row it had not removed, so a section costing 98 against a share of 133 read as 149, and a page of 381 characters became 373 with five rows gone. Pricing each section as it stands is the correction. It landed in commit 07540892eafe6e8a927a70bd10e9946a9e74226d under TASK-ecf0f37f68c9, which carries that commit as its proof, so the work is already accounted for. tests/golden-json/context.json returned to the one-of-everything document with no fixture change at all, and it is compared byte for byte at the budget golden_repo declares, so the page that fits is pinned there.

The other half must not be built. This task asks that a cut which does not reduce the rendered page is not taken, and measurement says otherwise: a +n notice does cost more than the first row it replaces, but it is paid once per section and every later row after it is pure saving, so the guard leaves a genuinely oversized page entirely uncut. Built as filed, a corpus of twelve proposals and twelve tasks stayed whole at a budget of 400. The counters +12 not shown and +8 more tasks were pinning correct behaviour rather than the defect, and they did not move.

Nothing is left to build, so this closes rather than completes.
