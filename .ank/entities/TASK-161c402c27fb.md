---
id: TASK-161c402c27fb
type: task
slug: ank-update-check-reads-the-latest-release-with-g
title: ank update --check reads the latest release with git ls-remote and installs nothing
created: 2026-09-13T17:19:12Z
author: claude-code/opus-5+planning
status: open
scope:
  - crates/ank-cli/src/**
  - crates/ank-contract/src/verbs.rs
  - crates/ank-cli/tests/**
blocked_by: [TASK-fdf872f98bf4]
done_criteria: |
  Through the binary, against a local bare repository standing in for the release repository: ank update --check prints the running version and the latest release, where the latest is the highest tag of the exact form vMAJOR.MINOR.PATCH that git ls-remote --tags --refs returns, other tags ignored; it exits 0 when the running version is at or above it and 8 when a newer release exists, and it starts no process but git. --json carries current, latest and newer as a boolean. A remote that cannot be reached exits 9 naming the repository it asked. No other verb starts git ls-remote, proved by counting the processes of a run of ank status and ank context with GIT_TRACE. update is removed from NOT_YET_DISPATCHED in the commit that dispatches it, ank help --json carries update with --check and --version, and the help golden is re-blessed.
criteria_by: creator
verify: [cargo-test, fmt-check]
schema: 4
version: 1
---

ADR-64f32c74a0f9 once accepted. The repository asked defaults to the one the
release is published from; the test needs a seam to point it at a local bare
repository holding tags. Pick one that is also honest in production (a mirror is
a real use), name it in the verb's help, and prove it through the binary rather
than through the function.

Versions compare numerically per component: v0.10.0 is above v0.9.0. The
running version is the package version the binary was built with, which is also
what --version prints first.

Measured 2026-09-13: `git ls-remote --tags --refs https://github.com/haksolot/ank`
returns v0.5.0, v0.6.0 and v0.7.0. ls-remote is plumbing ADR-9307e5d214a7 allows.
