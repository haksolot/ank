---
id: TASK-76196531ed9c
type: task
slug: the-skills-and-claude-md-describe-the-exit-codes
title: The skills and CLAUDE.md describe the exit codes, status and verifiers as the binary has them
created: 2026-09-19T18:26:33Z
author: claude-code/opus-5+docs-audit
status: open
scope:
  - skill/**
  - CLAUDE.md
blocked_by: []
done_criteria: |
  skill/SKILL.md's exit-code line agrees with crates/ank-contract/src/exit.rs for every code an agent meets (it includes 5 and 7, and says 6 also means no claim held), lists the status fields ank status prints, and does not say status names a stale code base unless it does; skill/loop/SKILL.md does not say done takes a proof when verify: is declared; skill/plan/SKILL.md says criteria are enforced at claim, not at new. CLAUDE.md names all five activity skills, says a plain ank new task already carries the default verifiers, and names --no-verify as how verify: is left empty. The skill hash ank --version reports matches the files after the change.
criteria_by: creator
verify: [cargo-test, fmt-check]
schema: 4
version: 1
---

Findings from the 2026-09-19 audit, measured with ank 0.8.0: a missing claim exits 6; a failing verifier and --proof on a verified task exit 5; an unknown --verify, a second claim and a claim on a task with no criteria exit 7; status reported 'drift none, level with main' on a branch one code commit behind main. The skills are embedded by build.rs, so the default verifiers stay: the suite is what proves the embedded copy and the revisions still agree.
