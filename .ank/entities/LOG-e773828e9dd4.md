---
id: LOG-e773828e9dd4
type: log
title: "The evidence first, because the task rests on it: run 32587758407 attempt 1 failed and attempt 2"
created: 2026-08-22T17:52:00Z
author: claude-code/opus-5
scope:
  - crates/ank-cli/src/commands.rs
  - crates/ank-cli/src/claim.rs
about: TASK-53ed7978621b
seq: 0
schema: 4
version: 1
---

 succeeded on the same sha f290c42, nothing in the tree between them. The mechanism is exact rather than suspected. renew writes expires = now_secs() + ttl and stores ttl beside it, so both halves of what it did are on the ref. The old assertion recovered neither: it subtracted the clock at the moment of reading and required the difference within two seconds of the lease, which put the rest of the verb, two git spawns for the ref update and whatever else the runner was building inside a two-second budget. The replacement reads ttl and expires - ttl, brackets the second between two clock reads the test takes itself, and asserts nothing about elapsed time. The bracket widens with the load instead of narrowing, so a slow machine makes it looser and never false. Measured: three seconds of sleep inserted between the call and the read and the new assertion still passes, where the old one would have computed 57 against a window of 58 to 62.
