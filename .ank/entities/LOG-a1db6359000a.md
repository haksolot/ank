---
id: LOG-a1db6359000a
type: log
title: "discrepancy found by the workspace suite, and fixed in this pass: (1)"
created: 2026-09-13T12:49:23Z
author: claude-code/544e
scope:
  - crates/ank-cli/build.rs
  - crates/ank-cli/src/**
  - crates/ank-contract/src/verbs.rs
  - crates/ank-cli/tests/skill.rs
about: TASK-544ec9655570
seq: 3
schema: 4
version: 1
---

 no_verb_puts_anything_but_json_on_stdout_under_json read prose from 'ank skills --json', which section 4 forbids; the criterion asks for no document, so the verb refuses --json at exit 1 and declares it in refuses_globals rather than committing a contract the plan never designed (a document can be added later without breaking a caller). (2) every_argument_on_the_surface_is_classified_as_carrying_a_path_or_not required --install in NOT_A_PATH (tests/cli.rs, outside the declared scope, forced by the new flag). (3) The suite's binary carried no skills: after the no-skill/ measurement build, restoring skill/ did not rerun build.rs, because a build with no skill/ emits no rerun-if-changed for it and the git refs are all it watches. build.rs emits only existing paths on purpose (a missing path reruns every build), and this predates the task for skill/SKILL.md; it bites only a tree whose skill/ disappears and comes back. Worked around by touching build.rs.
