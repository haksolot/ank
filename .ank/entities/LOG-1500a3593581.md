---
id: LOG-1500a3593581
type: log
title: Fixed, and each half measured against the cause it answers.
created: 2026-09-20T18:52:28Z
author: claude-code/opus-5+flake
scope:
  - crates/ank-cli/src/index.rs
  - crates/ank-cli/tests/**
about: TASK-b9701a228f47
seq: 6
schema: 4
version: 1
---



Cause 2, Index::open_as: contention now returns instead of deleting, the same exemption open_raw already made by name.

Cause 1, Index::refresh_counted: the decided writes are re-read against the files table *under* the IMMEDIATE lock and the ones another process already made are dropped; an empty list rolls back without writing. known_files became a free function over a Connection so the transaction can ask the same question, which is the shape ensure_schema already uses.

Counted with ANK_INDEX_REFRESHED, 4 concurrent readers of a cold copy: one wrote 'hashed=2269 indexed=2269', the other three 'hashed=2269 indexed=0'. Before the fix all four wrote indexed=2266. Eight sequential rebuilds became one.

The regression test: 5 runs FAILED before, 8.0-8.9 s each; 6 runs ok after, 2.2-2.6 s each. The wall clock is the same fact from the other end -- the eight rebuilds collapsed into one.

The original reproduction, this repository's own corpus, index.db removed before each round: 5 rounds of 16 concurrent readers, 80 of 80 exit 0, no error of any kind, where the same rounds before the fix refused 13, 13 and 12 of 16. Pushed to 32 concurrent readers, 3 cold rounds, 96 of 96 exit 0.
