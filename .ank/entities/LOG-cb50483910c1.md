---
id: LOG-cb50483910c1
type: log
title: Done, and the schema bump was the decision the criterion did not name. LogFm denies unknown fields,
created: 2026-08-21T22:17:54Z
author: claude-code/opus-5
scope:
  - crates/ank-core/src/model.rs
  - crates/ank-core/src/parse.rs
  - crates/ank-core/src/registry.rs
  - crates/ank-cli/src/human.rs
  - crates/ank-cli/tests/cli.rs
about: TASK-027a429aad2e
seq: 0
schema: 4
version: 1
---

 so a new field means an older build reads "unknown field records" and goes hunting for a typo, which model.rs names as the exact failure the reader range exists to prevent. SCHEMA_VERSION moves 3 to 4, every entity a verb writes afterwards carries 4, and ank 0.5.0 already installed goes progressively blind to a corpus one entity at a time. That is the designed behaviour of the constant and it is a real price, so it is written where the constant lives rather than left for someone to discover. The human chose it over the alternative, a convention on the message costing no bump, and the reason it is the right call here is TASK-dfe5a1bb0857: check will count these entries against the version an entity carries, and arithmetic wants a field rather than a prefix.

The split is at the reader and never at the source. entries::about still answers with every entry, because an entry is an entity and a listing that dropped some of them would be a corpus that reads differently through two verbs. What changed is presentation: show and log print a section of their own under the work trace, context serves the trace alone because it is budgeted for an agent about to work, and the LOG count is the trace's.

Four things the existing tests taught me, each of which I would have got wrong alone. every_golden_conforms_to_the_shape_its_verb_declares refuses a declaration no fixture reaches, so declaring show.machinery forced me to seed a machinery entry in the golden corpus before the declaration was allowed to exist. The conformance suite demands a valid fixture at every schema in the reader range, so the bump forced a schema 4 file that round-trips. opt() in the contract means nullable and not absent, so records is emitted on every entry, null on the work trace. And canonical form quotes a scalar only where YAML requires it, so my seeder writing every title quoted produced files check called non-canonical, which cost me three passes to see.

One thing worth carrying to TASK-3c12e0ced2c0: a message containing a colon is written quoted by the serializer and round-trips, verified by writing one through the binary. So the edit message may hold "constraint, body: 1 -> 2" without special handling.

SPEC-a89ba4f755e9 supersedes the data model with three edits: the registry row, the version sentence, and one paragraph beside the message-convention record it deliberately is not. Ratification will make four documents cite a superseded id, the same housekeeping the last supersession left.
