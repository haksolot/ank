---
id: TASK-b858b5965b07
type: task
slug: the-corpus-has-no-declared-weight-and-nothing-sa
title: The corpus has no declared weight, and nothing says when it outgrew one
created: 2026-09-14T06:40:13Z
author: haksolot@vmi3223161
status: in_progress
scope:
  - crates/ank-cli/src/config.rs
  - crates/ank-cli/src/human.rs
  - crates/ank-cli/src/init.rs
  - crates/ank-contract/**
  - crates/ank-cli/tests/cli.rs
  - docs/**
blocked_by: []
done_criteria: |
  config.yml accepts weight: {hot_files, plane_bytes}; init writes the two with defaults; a corpus over either is one check signal naming the counter, the value, the budget and the command that resolves it, and a corpus under both reports nothing new; check --json gains the two counters as additive fields, none retyped; the counts come from the walk and the batch check already performs, verified by the git process count not rising. cargo test --workspace, cargo fmt --check and ank check stay green.
criteria_by: creator
verify: [cargo-test, fmt-check]
method: tdd
schema: 4
version: 2
---

The decision is ADR-306fdb75e265. On 2026-09-14 this corpus is 1921 hot entity files
and 7.5 MB of refs, and the only way to learn either was to time a verb.
The attention budget of §5 is the model: a number in `config.yml`
(`context_budget`, `config.rs:131`), a signal when it is crossed
(`human.rs:1999-2045`), and the fact the corpus outgrew in the message.

`hot_files` counts the entity files under `.ank/entities/`; `plane_bytes`
counts the bytes of the objects under `refs/ank/`, which `check` already
batches. The resolving commands are `ank archive` for the first and the
compaction or deletion the plane's own signals name for the second.
ADR-cc65f1388a71: no new process.
