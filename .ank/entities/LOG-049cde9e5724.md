---
id: LOG-049cde9e5724
type: log
title: "Built. compose(here, fields, width) in view.rs is the one assembler: a Field is what it says, the"
created: 2026-08-28T20:26:31Z
author: claude-code/opus-5+one-row-composer
scope:
  - crates/ank-tui/src/view.rs
  - crates/ank-tui/src/text.rs
about: TASK-d58efe37eb7b
seq: 1
schema: 4
version: 1
---

 column it is padded to, the role the shared table gave it and whether the row may drop it. Field::id decides the identifier's column, its shortening and that it is kept, so no call site can get any of the three wrong -- that is what the five schemas were disagreeing about. The rightmost droppable field goes first, which drops the title, then the status, then the entities' row number, and leaves the marker and the identifier; fitted() past that is the clamp and not the policy. All five listings reproduce their old output byte for byte at every width the harness opens, which is why no pty test moved. Two mechanical tests, on paint.rs's pattern and both verified to bite by planting a violation: the walk over src/ fails when any code outside compose() calls Composed::column, and compose() itself may name no screen, so a single composer cannot become the five schemas again in one body.
