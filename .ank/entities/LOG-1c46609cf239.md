---
id: LOG-1c46609cf239
type: log
title: The falsification, recorded before the fix exists.
created: 2026-09-20T18:50:03Z
author: claude-code/opus-5+flake
scope:
  - crates/ank-cli/src/index.rs
  - crates/ank-cli/tests/**
about: TASK-b9701a228f47
seq: 5
schema: 4
version: 1
---



concurrent_readers_of_a_cold_corpus_of_this_size_all_answer, crates/ank-cli/tests/cli.rs. Eight threads, each spawning ank against a cold copy of this repository's own corpus (2269 files), all asking for the archive. Run five times against the unfixed tree, five FAILED, 8.0-8.9 s each. One failure verbatim:

    readers of a cold corpus of this size refused each other.
    #1 `ank find --type adr --all --json` exited 1: error[1]: index: disk I/O error
      -> rm /tmp/ank-it-2022568/ank-cli-it-2022568-0/.ank/index.db
    #3 `ank find --status superseded --all --json` exited 1: error[1]: index: attempt to write a readonly database
    #4 `ank find --type adr --all --json` exited 1: error[1]: index: attempt to write a readonly database

The first shape of this test was green six times out of six, and the difference is worth keeping: it gave the eight readers a mix of verbs, only two of which asked for the archive. An asking open walks 2269 files and a plain one 715, so with six cheap readers the queue never reaches the five-second wall. Measured from the shell on three fresh copies each way: eight asking readers refused 3, 3, 3; eight on the mix refused 0, 0, 0. A corpus of twelve seeded tasks, which is what the two tests already here use, never queues at all -- which is why they were green through both reports.
