---
id: LOG-279d50882d86
type: log
title: The reading, verb by verb, code against declaration. Six of the seven refuse on states they declare
created: 2026-08-20T04:08:46Z
author: claude-code/opus-5
scope:
  - crates/ank-contract/**
  - crates/ank-cli/src/cli.rs
about: TASK-106dccc7f71c
seq: 0
schema: 3
version: 1
---

 nowhere; one is honest.

context: refuses a path that names nothing in the repository (context.rs:1586 -> normalised, code 1) and a --limit that does not parse as a number (context.rs:1576, code 1). Declares neither.
find: refuses an unknown --type value (commands.rs:1117, code 1) and a --scope naming nothing in the repository (commands.rs:1134, code 1). Declares neither. --status is deliberately not validated: an unknown value filters to nothing and exits 0, which is a listing and not a refusal.
status: takes no path, validates no value, and raises no CliError of its own. --remote reads an unreachable origin as a warning and answers locally, which the table already explains beside coordinates. Genuinely refuses on nothing.
review: refuses a path that names nothing in the repository (human.rs:3118, code 1). Declares nothing. Exit 8 is findings, not a refusal, and the note already carries it.
graph: refuses a path that names nothing in the repository (graph.rs:25, code 1). Declares nothing.
scope: refuses a missing positional path (commands.rs:1414, code 1) and a path that names nothing in the repository (commands.rs:1422, code 1). Declares neither. The positional is mandatory in the usage line and the parser does not enforce mandatory positionals, so the verb does it itself.
check: refuses a path that names nothing in the repository (human.rs:197, code 1). Declares nothing. Exit 8 is findings, already a note.

All measured through the binary against this repository, not read off the source alone.

The path refusal is not a new rule: SPEC-4eff already states it in full, "A path naming nothing inside the repository, because it is absolute or because it climbs above the root, is refused with the command to run next; it is never answered", and extends it to flag values by name including find --scope. So the table is behind the specification rather than the specification being silent, and declaring these rows changes no behaviour.

What errors bubble out of Index::open or a corpus that does not parse is left alone: every verb has it, no verb declares it, and it is a property of the shared reading layer rather than a state any one verb refuses on.
