---
id: LOG-e70995356183
type: log
title: "After, measured the same way on the binary rebuilt from this branch: 8 of 8 coordinating verbs now"
created: 2026-08-31T03:55:24Z
author: claude-code/opus-5+verbs
scope:
  - crates/ank-contract/src/verbs.rs
  - crates/ank-cli/tests/cli.rs
  - crates/ank-cli/tests/golden-json/help.json
  - crates/ank-cli/tests/golden-json/help-verb.json
about: TASK-f3eb2695799a
seq: 2
schema: 4
version: 1
---

 carry a line naming what a refused push does to the exit code. claim, log, done, release and close carry PUSH_DEGRADES; attest carries PUSH_FAILS beside the refusal that names the --detached path; accept and init carry PUSH_NONE, because measurement says neither writes a ref at all -- accept's product is a signed commit and it left refs/ank/ untouched with the remote unreachable. One sentence per class, named from the table rather than written out eight times, so a verb picks a side instead of describing one. every_coordinating_verb_says_what_a_refused_push_does walks COMMANDS through `ank help <verb> --json`, keyed on coordinates: true rather than on a list of names, and is red-first both ways: dropping the note from release fails it (states 0 of the three push classes), adding one to show fails it (a verb that never reaches a remote). One residue, outside this scope and not fixed: the ADR says a degrading verb 'degrades, warns and exits zero', and release and close exit 0 without warning -- the notes say what the verbs measurably do, which is exit 0, and do not claim a warning that is not printed.
