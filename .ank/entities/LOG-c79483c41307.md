---
id: LOG-c79483c41307
type: log
title: "decision, the order of an entity's entries: the kind gains a required field seq, the rank of the"
created: 2026-08-15T13:08:05Z
author: claude-code/log-entities
scope:
  - crates/ank-core/**
  - crates/ank-cli/**
  - docs/**
about: TASK-166626ed8095
seq: 1
schema: 3
version: 1
---

 entry among the entries about the same subject counting from 0, and the order is created then seq then the identifier. Every part is read off the entity. A writer sets seq to one more than the highest it can see, which is a bounded read and the same query it needs to display them; a migrated entry takes the 0-based index of its line in the file it came from. Two alternatives were weighed and both are worse. A finer created would fix new writes and could not recover the past, because a migrated corpus carries second-resolution instants and refining them would invent precision that was never recorded, which section 3 forbids in as many words. Grinding the entropy of an identifier until the hash happens to sort in line order works and costs no field, and it makes the identifier carry meaning the specification says it does not, so the first id allocated any other way would break the order in silence. What seq does not promise matters as much: it is not a clock and not a lock, two writers who cannot see each other produce the same value, and that is the honest answer because they were concurrent. The merge is still two new files.
