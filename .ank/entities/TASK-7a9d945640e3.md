---
id: TASK-7a9d945640e3
type: task
slug: cli-rs-carries-its-own-copy-of-the-golden-redact
title: cli.rs carries its own copy of the golden redactor
created: 2026-08-31T08:57:34Z
author: claude-code/opus-5+golden
status: open
scope:
  - crates/ank-cli/tests/cli.rs
  - crates/ank-cli/tests/fixture/**
blocked_by: []
done_criteria: |
  crates/ank-cli/tests/cli.rs captures its fixtures through crates/ank-cli/tests/fixture/mod.rs, so one redaction serves every golden in tests/golden-json/ and deleting the copy in cli.rs leaves the suite green.
criteria_by: creator
verify: [cargo-test, fmt-check]
schema: 4
version: 2
---

Left behind by TASK-49b10f02d209, which added the two fixtures ADR-6fd69efb629c
asked for and could not capture both from `cli.rs`: `tui`'s document exists only
on a terminal, so it is captured in the pseudo-terminal suite, and an
integration test is a crate of its own -- `cli.rs`'s `golden` and its `redact`
are not reachable from `tui.rs` or `schema.rs`.

So `crates/ank-cli/tests/fixture/mod.rs` carries the second copy of the two
masks a captured document needs, and `cli.rs` still carries the first. Two
redactions that must agree is the shape ADR-6fd69efb629c spends its first page
on -- four escapers that did not agree -- one rung down, in the suite rather
than in the tool. Nothing is wrong today: the copy was made faithfully and the
fixtures it writes are the same kind of file as the twenty-six beside them. What
is missing is the thing that keeps that true, because nothing fails when the two
drift apart.

It was not done at the time on purpose: `cli.rs` was a perimeter three other
agents were working in, and that task's footprint in it was one number.
