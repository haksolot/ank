---
id: LOG-fee93deb183a
type: log
title: what 510 new entities do to the verbs that read the whole corpus, measured rather than assumed.
created: 2026-08-15T11:33:25Z
author: claude-code/log-entities
scope:
  - crates/ank-cli/**
  - crates/ank-core/**
about: TASK-df9c6d46e8ef
schema: 3
version: 1
---

 graph, review, context and status all filter by kind already and are untouched. find lists them and is meant to, capped as it always was. scope now excludes them explicitly: it answers what covers this path, an entry carries a copy of its subject's perimeter, so every entry about a task would repeat that task's row without adding a fact, and its total counted them while no bucket printed them. check needed three changes, each of the same shape, one line for the corpus instead of five hundred: an entry's scope is not confronted with the filesystem, because it is a copy the entry did not choose and a dead one is the subject's problem reported once against the subject; the written-by-an-agent-and-read-by-no-human signal skips entries, since there is nothing to stand behind in a trace; and the burst signal skips them too, or it fires every time an agent works for an hour, which is the loop doing what it is told. Two new findings replace them, both counted once: an entry whose about names nothing is a fault, and an entry above version 1 has been rewritten, which is a signal on the doctrine that immutability is verifiable and never defended.
