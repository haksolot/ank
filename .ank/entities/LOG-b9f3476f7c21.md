---
id: LOG-b9f3476f7c21
type: log
title: "Measured 2026-09-15. Real route, Linux: a copy of this build (ank 0.7.0, 617f0e0, skill"
created: 2026-09-15T08:45:40Z
author: claude-code/opus-5+update-verb
scope:
  - crates/ank-cli/src/**
  - crates/ank-contract/src/verbs.rs
  - crates/ank-cli/tests/**
about: TASK-1c8c100554a1
seq: 3
schema: 4
version: 1
---

 b991532258ba) outside target/ ran ank update --version 0.6.0; it printed 'installing 0.6.0 over 0.7.0: curl -fsSL https://raw.githubusercontent.com/haksolot/ank/main/install.sh | sh -s -- --version v0.6.0 --dir <its dir> --no-welcome', install.sh verified checksum 497ebd75..., the file in place became ank 0.6.0 (f745362, skill c5bae2a5f350), update printed 'the skills changed (b991532258ba to c5bae2a5f350): ank skills --install installs them' once, exit 0. The same copy with no --version against the real repository: up to date, exit 0, exactly 1 git built-in (ls-remote) under GIT_TRACE. target/debug/ank update: exit 7, hint cargo build. CI on PR #444 (2e9919f): update.rs 20 tests green on ubuntu, macos-latest, macos-15-intel and windows-latest; on windows-latest the_running_executable_is_renamed_aside_on_windows_only passed, i.e. the powershell stub found no ank.exe in the directory at start. Two suite races found and fixed in the harness, not the verb: ETXTBSY on freshly written executables (retried, as skills.rs does) and curl/sh stub records interleaving on macOS (each stub now appends in one write). Choices: curl and sh are two processes joined by a pipe ank makes, so no shell parses the directory; Windows hands install.ps1's README line to powershell as -EncodedCommand padded to carry no '=', so no cmd or PowerShell quoting reaches it; the cargo build is recognised by cargo's CACHEDIR.TAG in an ancestor, not by a directory named target; npm is recognised by node_modules/@haksolot/ank among the ancestors (a non-global npm install, which hoists the platform package, falls to the installer route: out of this criterion). The skill-revision-differs case is POSIX-only (the installed binary is a script); Windows runs the same-revision case.
