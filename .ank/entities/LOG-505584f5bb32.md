---
id: LOG-505584f5bb32
type: log
title: "The successor carries the predecessor's document whole, built rather than retyped: its body is"
created: 2026-08-24T18:24:18Z
author: claude-code/opus-5-reading
scope:
  - crates/ank-cli/tests/skill.rs
about: TASK-d055f8ab836b
seq: 0
schema: 4
version: 1
---

 SPEC-1012d307caff's with two edits, one line added to the Commands block and one passage added before the amend paragraph. A spec's ratification anchor covers the body as well as the scope, so amend refuses and a succession is the only road; reproducing 84 KB by hand would have been 84 KB of chances to change a word nobody meant to change.

read joins the block after accept, which fixes both orders at once: tests/skill.rs holds ank help to section 4's order inside each group, and the contract table has to place the verb between accept and close to match. Beside accept because that is where it was split from: the reading a ratification records is this act performed as a consequence of another.

Two things the criterion did not have to settle. It refuses a log entry, because ADR-25f977377fa0 says an entry is written once and never modified, and check already excludes entries from the unread signal for the reason that decides this one. And the contract version does not move: its own doc says a document may gain a field within a version and may never lose, rename or retype one, so a new verb adds a document and breaks no caller that binds an existing one. It has been 1 since it existed.

NOT_YET_DISPATCHED goes from empty to one entry, and its doc comment goes with it: it said every verb section 4 specifies now ships, which stopped being true the moment the successor was written. The list's own history is the argument for this shape, scope, graph, status and edit each having come through it.
