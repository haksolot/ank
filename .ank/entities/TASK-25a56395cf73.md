---
id: TASK-25a56395cf73
type: task
slug: check-resolves-a-reference-a-supersession-a-bloc
title: check resolves a reference, a supersession, a blocker and a log subject against the archive
created: 2026-09-14T13:18:05Z
author: haksolot@vmi3223161
status: in_progress
scope:
  - crates/ank-cli/src/human.rs
  - crates/ank-cli/src/store.rs
  - crates/ank-cli/tests/cli.rs
blocked_by: []
done_criteria: |
  On a corpus where a superseded spec, an ADR another ADR supersedes, and a task another task is blocked by have been moved by hand into .ank/archive/entities/ together with the entries about them, check reports no fault and no finding naming them as nonexistent: a reference, a supersession, a blocked_by and a log subject that name an archived entity resolve, the archived entity is still verified by digest, and the prose-identifier signal does not count an archived id as naming nothing. The whole-corpus-read guard keeps its meaning: an archive that exists does not silence 'nothing supersedes this'. Asserted through the binary. cargo test --workspace, cargo fmt --check and ank check stay green.
criteria_by: creator
verify: [cargo-test, fmt-check]
method: tdd
schema: 4
version: 2
---

The reading half TASK-da978b214eca left out, on purpose and measured
(LOG-91bf1a8abb90): check_references, check_succession, check_entries,
check_prose_identifiers and the blocked_by resolution read the parsed hot
entities plus the unread set, and the unread set doubles as the
whole-corpus-read guard (unread.is_empty()), so archived ids need a set of
their own beside it: Store::archived_ids() already lists the archive by
file name with no parse. The rule is ADR-467ce7e9cda1. Blocks TASK-97fd1992567a,
which would otherwise turn 31 faults on the day it first runs here.
