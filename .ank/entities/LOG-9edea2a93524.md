---
id: LOG-9edea2a93524
type: log
title: Three things the criterion left to the code, all decided by reading a ratified constraint rather
created: 2026-08-24T19:09:17Z
author: claude-code/opus-5-reading
scope:
  - crates/ank-cli/**
  - crates/ank-contract/**
about: TASK-e3370ef322d8
seq: 5
schema: 4
version: 1
---

 than by taste.

No log entry, and no machinery entry either. accept records a reading and writes no entry: the reading is the record, on the entity, carrying the actor and the instant, and a line saying the same thing in the work trace would be one fact written twice. The machinery entry is settled by ADR-f7dc76886db2, which puts verified on the transition side of its own definition of content, beside status, proof and ratified, so the write leaves the content hash where it was and check's version accounting is never reached.

coordinates: false. Nothing here touches a ref, a branch or a remote, and record_entry needs only the store and the index, so ADR-9307e5d214a7 puts this verb on the files-alone side. renews: Named, the same answer show gives, because reading the entity you hold is work on it.

And the refusal on a log entry names the rule rather than the kind: an entry is written once, so there is no second write for a reading to be.

Two accounting numbers moved with the surface, both caught by tests rather than by reading. COMMANDS.len() is 23 where its own message says the number moves only when section 4 does, and it just did. And the help --json golden gained the read entry: I read the diff before blessing it, and it adds that entry and removes nothing, which is a verb arriving and no contract breaking.

Falsified where it mattered: check reports the entity nobody read and stops reporting the one human:marie read, asserted in the same run so that a rule which stopped checking altogether fails.
