---
id: LOG-295da87e320f
type: log
title: The SVG was replaced by a GIF, and the reason was measured rather than preferred. svg-term renders
created: 2026-08-16T21:37:01Z
author: claude-code/opus-5
scope:
  - README.md
  - assets/**
about: TASK-ab3592c8c586
seq: 1
schema: 3
version: 1
---

 a cast as every frame side by side in one strip, slid by a CSS animation. Verified in Chrome both ways on the pushed branch: injected inline into the page the animation is running and currentTime advances 3167 ms in 3 s; loaded through an <img>, the same file sits on frame zero, which is an empty terminal after the clear. prefers-reduced-motion was false, so that is not it. A README can only embed an image, so the SVG showed nothing at all -- which is what the maintainer saw and reported as missing content.

agg renders the same cast to a GIF that animates wherever an image does, and it takes the palette as hex, so Catppuccin is exact rather than substituted after the fact. render.sh lost the colour-mapping code and the check that guarded it, because there is nothing left to map. 250 KB per theme, which is smaller than the SVG pair it replaces. One default had to be overridden: agg caps idle time at five seconds, which would have cut the 8.5 s pause on the constraint -- the beat the recording exists for -- so --idle-time-limit is set above the longest pause play.sh takes.

Two things the retake fixed beyond the format. ank graph is in the recording now: the demo repository was built with a dependency tree on purpose and nothing showed it. And the refusal reads "unknown environment 'stagin', try: depot deploy staging" rather than listing all three environments separated by pipes -- the constraint on screen promises exactly that string, and the beat where the code visibly obeys is weaker when the output is noisier than the promise. The demo now suggests the nearest match, and the test asserts the whole message with assert_eq rather than a contains.

play.sh keeps to the eight ANSI colours. A 38;5;183 in the prompt came out of xterm's cube and was the one thing on screen the theme could not reach.
