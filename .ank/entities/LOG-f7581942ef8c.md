---
id: LOG-f7581942ef8c
type: log
title: Green, and measured by count (ANK_INDEX_REFRESHED knob, one line per refresh). This repository's
created: 2026-09-14T10:13:45Z
author: claude-code/opus-5+cold-rebuild
scope:
  - crates/ank-cli/src/index.rs
  - crates/ank-cli/tests/cli.rs
about: TASK-a4565686c619
seq: 5
schema: 4
version: 1
---

 own corpus, 1983 entity files, debug binary: first find --json after the schema 7->8 bump hashed=1983 indexed=1983; the next two find --json hashed=0 unchanged=1983; status refreshes twice, both hashed=0. Fixtures: unit second open of 20 backdated files hashed 0 (was 20); binary find/context/graph over 30 backdated tasks hashed 0 (was 30). Mutations: dropping 'mtime < last_write' fails a_same_size_rewrite_is_trusted_only_strictly_before_the_last_write and its binary twin; comparing mtime and size but not inode fails a_changed_size_or_inode_or_an_unavailable_field_is_hashed and a_file_whose_size_or_inode_changed_is_hashed. last_write is the mtime of index.db read after the commit and recorded in a second statement, which leaves the recorded value no later than the file's real last write (conservative direction). A stat whose hash still matches but whose fields moved is rewritten (Write::Restat), so a touched file is hashed once and not forever; a racy file with an equal stat writes nothing, so warm readers still take no write lock.
