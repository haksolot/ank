---
id: LOG-9905eb254cf3
type: log
title: is_contention(ErrorKind, LockPlatform) is the pure decision, with the single cfg! at the edge in
created: 2026-07-31T16:45:41Z
author: claude-code@ank
scope:
  - crates/ank-cli/src/store.rs
about: TASK-dc87e0ecfb6c
seq: 0
schema: 3
version: 1
---

 LockPlatform::HOST. PermissionDenied now fails at once on posix as LockDenied, code 9, naming the directory and not the lock file: the lock is not the problem, the place we cannot create it in is. LockTimeout carries the last refusal so the post-deadline message tells a holder apart from a door that never opens, and only the holder case invites rm. The Windows branch is exercised from Linux by passing LockPlatform::Windows, which is the whole point of the parameter.
