---
id: LOG-c29b1786e781
type: log
title: measured before deciding, and the diagnosis is right but understates it. In a harness that writes
created: 2026-08-15T12:18:53Z
author: claude-code/log-entities
scope:
  - crates/ank-core/**
  - crates/ank-cli/**
  - docs/**
about: TASK-166626ed8095
seq: 0
schema: 3
version: 1
---

 four entries about one task and reads them back, 12 runs of 12 put every entry in the same second and 10 of 12 printed them in the wrong order, and the order printed is exactly descending identifier. One ank log costs 230 to 360 milliseconds here, so four of them inside one second is the ordinary case rather than the exotic one. It is not a platform difference: the two tests CI failed on fail 2 runs out of 6 on Windows too, macos passed one CI run and failed another, and the Windows job takes 4m54s against ubuntu 1m08s, which is why its writes straddle a second more often and why it passed by luck. So cargo test green on this machine was never evidence, and the flake rate over repeated runs is what has to be measured instead. The cause is what the briefing named: an append-only file carried insertion order for free, a set of files does not, and the identifier I fell back on is a hash of the act of creation which carries no order at all. Recorded here and not on TASK-df9c6d46e8ef because that task is done and log refused it on state, which is the tool saying a defect found in review is new work.
