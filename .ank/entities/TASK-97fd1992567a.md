---
id: TASK-97fd1992567a
type: task
slug: ank-archive-moves-what-is-cold-and-commits-nothi
title: ank archive moves what is cold, and commits nothing
created: 2026-09-14T06:40:14Z
author: haksolot@vmi3223161
status: open
scope:
  - crates/ank-cli/src/commands.rs
  - crates/ank-cli/src/archive.rs
  - crates/ank-cli/src/cli.rs
  - crates/ank-cli/src/human.rs
  - crates/ank-contract/**
  - crates/ank-cli/tests/cli.rs
  - docs/getting-started.md
  - CONTRIBUTING.md
  - skill/**
blocked_by: [TASK-da978b214eca, TASK-25a56395cf73]
done_criteria: |
  On a scratch corpus holding superseded and accepted documents, entries about the superseded ones, entries about tasks done on the default branch, entries about open tasks, and done tasks, ank archive moves exactly the superseded documents and every log entry whose subject is cold (about a superseded document, or about a task done on the default branch), moves nothing else, writes no commit, prints every id it moved, and --dry-run prints the same list and moves nothing; check is green after the move and reports, before it, one signal naming ank archive for a corpus holding a cold entity. The verb is in the contract table and the skill names it. cargo test --workspace, cargo fmt --check and ank check stay green.
criteria_by: creator
verify: [cargo-test, fmt-check]
method: tdd
schema: 4
version: 3
---

The decision is ADR-306fdb75e265; this task is the writing half. Cold is a superseded
document, and a log entry whose subject is a task done on the default
branch -- the predicate `check` already holds for completion refs
(`done_on`). A task is never moved. The move is a rename through the store,
so `git` sees it as one; no commit (§12), like `done`, and the result lands
by pull request like a ratification because it is a human deciding what
the hot corpus is. The recipe goes in CONTRIBUTING.md beside accept's.
