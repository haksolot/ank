---
id: LOG-7e007de89ac8
type: log
title: The walk now records both sides and subtracts. `conforms` took a single `&mut Vec<String>` of empty
created: 2026-08-20T06:07:10Z
author: claude-code/opus-5
scope:
  - crates/ank-cli/tests/**
about: TASK-fbdf25e30058
seq: 0
schema: 3
version: 1
---

 paths; it takes an `Arrays { empty, filled }` and pushes into whichever side the instance is on, so a declaration is unexercised only where no instance of it carries a row.

The key keeps the fixture stem, which makes the subtraction per document. Deliberate, and the comment says so: a verb with several output shapes declares several fields of one name, and letting config-read fill what config-write declares would conflate two declarations. Within one document a normalised path is one shape by construction, which is the case the defect was actually about, `help.verbs[].refuses` being one declaration met twenty-two times.

The pinned list is now empty and asserted as empty, not loosened. Falsified twice: emptying `adr` in scope.json, the only instance of that shape anywhere, reddens the test with `["scope.adr"]`, so a genuine gap is still named; and `status` keeps its empty refuses array while the test passes, which is the case that used to be misreported.
