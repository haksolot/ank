---
id: LOG-14c3898a8e3a
type: log
title: "review, against the criterion: new task --method writes the field after verify and before schema"
created: 2026-09-13T14:01:29Z
author: claude-code/e0d7
scope:
  - crates/ank-core/src/**
  - crates/ank-core/tests/**
  - crates/ank-cli/src/**
  - crates/ank-contract/src/verbs.rs
  - docs/format.md
about: TASK-e0d72ec220a1
seq: 7
schema: 4
version: 1
---

 (cli.rs new_writes_the_method_in_its_canonical_position_and_show_prints_it, both with and without verify) and show prints it; an unknown name, ank and ank-tdd exit 7 naming every sibling directory and write no task; amend --method tdd replaces the value on the done task and the edit entry reads method tdd (version ...); context after the claim prints METHOD diagnose beneath the criterion, once, none in orientation and none on a claimed task without a method; done closed green on the verifier with no method entry in the log; SCHEMA_VERSION asserted 4; TASK-4d1a6e3b9c07.md round-trips; three fixtures without the field parse to None; a new test compares every docs/format.md table to the registry order and to the form the serializer writes, and it caught method documented bare while written scalar, so the row now says scalar; help --json carries --method on new and amend and nothing else. Beyond the letter, and kept because the criterion's clauses need them: edit.rs names method in the fields an edit record lists, --method refused on new adr/spec and amend adr/spec rather than dropped, and the ank-tdd hint. Against the constraints (ank scope on every touched path): ADR-6fd6 help.json re-blessed, no contract version move; ADR-1f70 no short form added; ADR-63b5 spec was ratified first, fixture beside the code; ADR-52bb the amend writes its edit record; ADR-b915 tension logged above. Found and fixed by the workspace run: the flag-surface guards in cli.rs (NOT_A_PATH and valid_value) did not know --method. Windows: cargo test --workspace --no-fail-fast green, 0 failed; the tui and ank-tui pseudo-terminal suites are cfg(unix) and ran 0 tests here, and the new form gains one row (--method), which their windows of 34 and more rows should hold; CI decides.
