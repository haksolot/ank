---
id: LOG-3cd15523b0b8
type: log
title: Measured on this repository, release build, warm, idle. The walk yields 1365 files where it yielded
created: 2026-08-24T19:23:03Z
author: claude-code/opus-5-walk
scope:
  - crates/ank-cli/src/human.rs
about: TASK-0e5a00f98cfe
seq: 2
schema: 4
version: 1
---

 11847, against 1361 that git tracks: the four extra are untracked files of this tree, which is what the walk has always been about and is not what this task narrows. ank check goes from 1500-1546 ms, median 1534, to 1227-1283 ms, median 1258, five alternating runs, ranges that do not overlap.

The correctness half turned out to be the larger one, and I had it wrong in the task body. I wrote that no verdict in this corpus was wrong and that the anchoring of globset made that a matter of luck. Two verdicts were wrong. TASK-10b8a29fd853 and TASK-3109a736c255 both scope .claude/**, whose files were deleted in 264636c406b9, and the eight checkouts living under .claude/worktrees/ made that glob match. Both now report, as signals naming the deletion, and check stays exit 0.

What makes that finding solid rather than a judgement of mine: TASK-10b8a29fd853's own log entry, written when it deleted those files, predicted this exact finding and argued it was correct. The hiding came later, when worktrees started being placed under .claude/, and nothing announced it. A corpus lost a finding because of where somebody put a worktree.

The rule is the .git entry and never the directory's name. Skipping .claude/worktrees would fix the instance and not the rule: a sibling clone, a vendored dependency with its own history, a worktree placed anywhere else are the same fact. The entry is a file in a worktree and a directory in a clone, so what is asked is that it exists at all, and the test builds the file form because that is what git worktree writes.

Falsified with the guard removed: both tests fail, one on the scope reading alive off the other checkout and one on the warning naming its corpus.
