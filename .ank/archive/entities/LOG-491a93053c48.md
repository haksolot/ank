---
id: LOG-491a93053c48
type: log
title: Measured through the binary built at 74024e6, lab repo (2 specs, one superseded by hand; one task
created: 2026-09-14T18:07:15Z
author: claude-code/opus-5+plane-namespaces
scope:
  - crates/ank-cli/src/store.rs
  - crates/ank-cli/src/index.rs
  - docs/**
about: TASK-dc29247e1c19
seq: 4
schema: 4
version: 1
---

 done and committed on main; 6 log entries). Archive: check before -- one signal 'corpus: 5 hot entities are cold and belong in .ank/archive/entities/, with every entry about them (ank archive)'; archive --dry-run lists the superseded spec and the 4 entries about it and about the done task, moves nothing (git status empty); archive --json moved the same 5, HEAD unchanged, git diff --cached -M shows 5 R100 renames; a second run 'nothing is cold'. The entry about the proposed successor and the entry about the open task stay hot. find lists no archived row ('archived':false); find --all lists the old spec with '(archived)'; show and log <id> answer from the archive. check exit 0 after the move: the successor's reference is followed to its chain head, and an open task scoped to .ank/entities/<archived-id>.md draws no dead scope. Appending a line to the archived file: check exit 8, one fault 'archived file changed: its bytes no longer match the digest the index holds, and an archived entity is never edited (git checkout -- .ank/archive/entities/<id>.md restores it)'; restored, exit 0. Index (ANK_INDEX_REFRESHED): cold find hashed=5; warm find, graph --json, context hashed=0; a file whose mtime is set after the index's last write is hashed on every open (hashed=1); find --all hashes the archive on its first open (hashed=6, then 1, the future-mtime file). Weight: check --json carries hot_files:5 plane_bytes:0; ank config weight.hot_files reads 3000; at hot_files 3 one signal 'weight.hot_files: 5 entity files under .ank/entities/, over the budget of 3: ank archive moves the cold half, ...'; at 5 (equal) none. In the proof lab, plane_bytes 325 = git cat-file -s of the one refs/ank/proof ref; the refs/ank/remote-check and refs/ank/watch/origin/proof objects are not counted. Found by reading, recorded: check does not rebuild the index as ADR-1556aaffe0c5's last sentence says; it reads and parses every hot file itself, outside the index, and hashes every archived file in full, so it takes no stat shortcut; the successor states what the binary does.
