---
id: LOG-26159c5b1e0e
type: log
title: Registry landed and the golden suite is green, 22/22, with no fixture edited to fit the
created: 2026-08-13T04:46:40Z
author: claude-code@sean-laptop
scope:
  - crates/ank-core/**
about: TASK-7c1ff5035894
seq: 0
schema: 3
version: 1
---

 implementation. One assertion in golden.rs was sharpened rather than relaxed: it tested that the body did not contain the string '## Log' anywhere, which a body legitimately discussing the format trips; it now tests that no line of the body is the heading. The kind facts live in registry.rs alone: EntityKind is an index into KINDS, id parsing and prefix resolution loop over the table, and a test asserts the enum order equals the table order rather than trusting it. One serializer walks the table, so the field order is data. Adding verified to Task and Adr forced sixteen one-line additions in ank-cli construction sites, which is outside this task's scope and unavoidable when a public struct gains a field; one test there hardcoded 'schema: 2' and now reads SCHEMA_VERSION. ank check from the tree exits 0 on the real corpus with no round-trip or canonical-form fault, which is the schema 1 and 2 regression test on 176 entities rather than on fixtures alone.
