---
id: LOG-f75c2c665e74
type: log
title: "Successor written: SPEC-20357e21a45a, proposed, superseding SPEC-78134d2b3cf8, same title, same"
created: 2026-08-24T23:02:23Z
author: claude-code/opus-5+spec-tui
scope:
  - crates/ank-contract/src/verbs.rs
  - crates/ank-cli/src/cli.rs
  - .ank/entities/SPEC-78134d2b3cf8.md
  - .ank/entities/SPEC-20357e21a45a.md
  - crates/ank-cli/tests/skill.rs
about: TASK-8108e3771ba0
seq: 6
schema: 4
version: 1
---

 scope crates/ank-cli/** and the same four references. The body is the predecessor's byte for byte plus two changes: the line 'ank tui' between 'ank scope <path>' and 'ank check [<path>]' in the Commands block, and five paragraphs under the block that argue the declaration. The line carries no marker because that is the form the block already uses: 'read' sat here unimplemented until the landing that shipped it, and the paragraph above the block already says ank help prints these verbs minus whatever is not yet implemented. Nothing cites the predecessor: no spec carries SPEC-78134d2b3cf8 in its references field, so the amend --reference clause of the criterion is satisfied by there being no document to follow through. Scope gained .ank/entities/SPEC-20357e21a45a.md and crates/ank-cli/tests/skill.rs. The first is the successor's own file, added after the entity was created rather than before, which is the wrong order and is recorded rather than tidied away. The second is NOT_YET_DISPATCHED: the criterion does not require it, since the suite reads the ratified document and tui is not in it yet, but the ADR's own price paragraph and the const's own comment both name the case, and read was declared there while its ratification was in flight for exactly this reason. Without it the human's signed accept is what turns the suite red, on a branch where nobody is looking for it.
