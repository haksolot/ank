---
id: LOG-95d194b33e6c
type: log
title: "The fold follows TASK-e655d28c83cb's shape exactly: ank-daemon declares [lib] name=ank_daemon and"
created: 2026-08-25T19:54:40Z
author: claude-code/opus-5+watch-verb
scope:
  - crates/ank-daemon/**
  - crates/ank-cli/src/cli.rs
about: TASK-9dd22f2b0430
seq: 1
schema: 4
version: 1
---

 no [[bin]], ank-cli links it, and 'watch' joins COMMANDS after 'mcp' in the 'look around' group, which is §4's order. Two things are the watcher's own. warm::locate_ank is gone entirely -- the ANK_BIN/beside-me/PATH ladder existed because a sibling had to find the ank it was released with, and 'ank watch' IS that binary, so Address{exe} carries the dispatch's current_exe() the way ank_mcp::Address does. And the dispatch resolves no corpus at all, unlike tui and mcp: what watch keeps warm is what the reader declared in watch.yml outside every repository (ADR-96174f1ac2b7), so demanding a .ank/ under cwd would refuse the watcher exactly where a person starts it. USAGE and parse() are gone with the siblinghood -- ank help watch prints the four flags out of the CommandSpec, and the CLI parser refuses an unknown one; what stayed in the crate is Options::interval(), because 'is not a number of milliseconds' and '0 would spin rather than watch' are the watcher's refusals and re-deriving them in cli.rs would be a second answer. The suite moved to crates/ank-cli/tests/watch.rs for tests/tui.rs's mechanical reason (CARGO_BIN_EXE_ank is defined only for the package declaring the binary) and all 17 tests pass unchanged against the verb, stopping_the_daemon_changes_no_verbs_output_and_no_verbs_exit_code included.
