---
id: LOG-0610cb091ec2
type: log
title: (a) Task branches opened on a base whose .ank/ was already behind main's last .ank/ change at that
created: 2026-09-15T11:05:43Z
author: claude-code/opus-5+history-counted
scope:
  - .ank/entities/**
about: TASK-96bc559cf6d8
seq: 2
schema: 4
version: 1
---

 moment.

Rules. The base is the parent of the oldest commit on the branch's first-parent chain (git rev-list --first-parent --reverse M^1..M^2 | head -1, then ^1). Behind means at least one first-parent commit of main that touched .ank/ and was made at or before the moment is not reachable from the base: git rev-list --first-parent --count --until=<moment> 98999e3 ^<base> -- .ank/ (stale.sh). Counting commits rather than asking whether the last one is an ancestor matters for 3 branches cut from another task branch (#180 #341 #439), whose base already held the .ank/ content main merged. The moment is not recorded anywhere, because the claim ref is gone, so it is measured three ways, the first being the closest to "opened": column 6, the earliest trace of the agent on the branch (its first commit's author date, or an ank log entry about the task it added that is dated earlier); column 5, the first commit's author date; column 10, the PR's creation. How much of main moves at all: git rev-list --first-parent --count 98999e3 -> 670, of which git rev-list --first-parent --count 98999e3 -- .ank/ -> 617, so nearly every merge touches the corpus and behind on .ank/ is close to behind at all.

Counts, out of 326 task branches (for c in 6 5 10; do bash stale.sh $c > stale$c.txt; done).
awk '$2>0' stale6.txt | wc -l -> 19 behind at the earliest trace: 11 missed 1 commit, 7 missed 2, 1 missed 5.
awk '$2>0' stale5.txt | wc -l -> 41 behind at the first commit: 30 missed 1, 9 missed 2, 1 missed 3, 1 missed 5.
awk '$2>0' stale10.txt | wc -l -> 55 behind at PR creation: 37 missed 1, 15 missed 2, 2 missed 3, 1 missed 6.
No branch in the window was more than 6 corpus commits behind by any of the three.

What being behind cost. First CI run: python3 firstrun.py > firstrun.txt; awk '{print $2}' firstrun.txt | sort | uniq -c -> 304 success, 20 failure, 2 cancelled over the 326. join <(sort stale6.txt) <(sort firstrun.txt) | awk '{k=($2>0?"behind":"level"); t[k]++; if($3=="failure")r[k]++} END{print r["behind"]+0, t["behind"], r["level"]+0, t["level"]}' -> red first run on 1 of 19 behind branches against 19 of 307 level ones (at column 5: 3 of 41 against 17 of 285; at column 10: 4 of 55 against 16 of 271). Git conflict, from the sync-merge replay in the next entry: join <(awk '$2>0' stale6.txt | sort) <(cut -d' ' -f1 syncconf.txt | sort -u) | wc -l -> 4 of the 19 behind branches later resolved a conflict, against 10 of the 307 level ones (column 5: 8 of 41; column 10: 11 of 55). The four are #54 (.ank/tasks/TASK-8ebd6e02f125.md, one entity file edited by two branches), #99 and #269 (crates/ank-cli/tests/cli.rs, crates/ank-cli/tests/golden-json/help.json) and #139 (crates/ank-cli/src/context.rs): each is a file another task also edited, which is the overlap signal's ground and not a corpus read wrong. Whether a red first run was caused by a stale corpus is not measured here, only how rarely the two coincide: join <(awk '$2>0' stale6.txt | sort) <(sort firstrun.txt) | grep failure -> #322 alone, behind by 1 commit.
