---
id: LOG-5243c7ca5479
type: log
title: THE NOTE IS MEASURED AGAINST THE TABLE, THROUGH THE BINARY. crates/ank-tui/tests/note.rs runs 'ank
created: 2026-08-27T06:30:05Z
author: claude-code/opus-5+doc
scope:
  - crates/ank-contract/**
  - crates/ank-tui/**
about: TASK-a836fdb2fca2
seq: 0
schema: 4
version: 1
---

 help tui', parses the verb list off the front of the note and holds it to bindings::BINDINGS' verb rows plus bindings::FURTHER, both ways. Twelve names, which is ank::ACTS: the writing six, new, edit, config, and the three the x list opens. The note carries the list ahead of a ';' on purpose -- a list picked out of the middle of a sentence is a list picked out by a guess -- and the contract says so beside the note. Measured by mutation, rebuilding the workspace between each: config dropped from the note fails on 'the reader spells [config]'; 'check' added to the note fails on 'names [check]'; the old 'act on the entity the focused panel names' clause put back fails the second test.

THE SENTENCE HAD TO BE TRUE OF TWELVE AND NOT OF SIX. The first replacement I drafted said each verb is composed onto the screen and run only when the person confirms it, and that is false of config: pressing 'o' opens the pane by running 'ank config <key>' on the READING road, with no confirmation at all. What is true of every one of them is that none WRITES unconfirmed -- ADR-c07e2694f0e1's guarantee stated over the verbs rather than over a list of them, which is the shape the ADR itself asks for. The task warned not to write a sentence false of the eighth; the eighth was config, and it was false of it in the reading direction rather than the writing one.

NO COUNT SURVIVES IN THE NOTE OR IN view.rs. The second test refuses 'six', 'seven' and 'eight' on the note, because a number is a fact about the table that has to be retyped every time the table grows -- which is the defect this task exists for. view.rs's three remaining 'six verbs' comments were about the writing half and locally true; they are phrased as 'the writing half' now, so the file carries no count of the reader's verbs at all.

THE DIAGRAM IS THE FRAME MEASURED, NOT REDRAWN FROM MEMORY. I drove the built binary on a pseudo-terminal at 80x24 with the tests' own harness and read the rows back: three header rows (corpus line with [?] on the right edge, identity line, rule), the panels, and one note band row -- four non-panel rows, which is what LOG-1afc1b09f95b measured and what tests/chrome.rs counts against a budget of five. The diagram now draws exactly those and says the sketch is in the ASCII border set, which is what a terminal that declares itself dumb gets.

THE GOLDEN PINS THE HELP DOCUMENT AND HAD TO BE BLESSED. crates/ank-cli/tests/golden-json/help.json carries every verb's notes, so a note edit in ank-contract turns crates/ank-cli/tests/cli.rs's json_golden_reading_verbs red. ANK_BLESS_GOLDEN=1 cargo test -p ank-cli --test cli -- json_golden, then read the diff: the file is one line, so 'git diff' shows the whole of it and the only way to see what moved is to substitute the old note into the old file and compare. Only the note differs.

BOTH WAVE TRAPS HELD. Every mutation check above was run after 'cargo build --workspace', never off 'cargo test -p ank-tui'; and note.rs names bindings from a tests/ file, so ank.rs's include_str! scan for 'bindings::' is untouched. note.rs is also the one suite in this crate with no '#![cfg(unix)]': it declares no terminal/mod.rs, because that module is unix-only and what this measures -- a page written to a pipe against a static table -- has no platform. The cost is a ten-line copy of the binary lookup, stated in its doc.
