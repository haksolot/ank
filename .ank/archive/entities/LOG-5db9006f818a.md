---
id: LOG-5db9006f818a
type: log
title: surveyed what still expects .ank/log/ before removing its creation. Nothing depends on init making
created: 2026-08-31T04:13:05Z
author: claude-code/opus-5+acaf
scope:
  - crates/ank-cli/src/init.rs
  - crates/ank-cli/tests/cli.rs
about: TASK-acaf2c3159dd
seq: 1
schema: 4
version: 1
---

 it. docs/format.md is explicit in two places ('A reader must accept all of them; a writer must never produce them', and 'Read them there; write neither'), and the accepted storage spec says the same: 'a reader accepts a .ank/log/ a former release wrote, a writer never produces one'. Readers stay: store.rs:738 lists ids in LOG_DIR, entries.rs unions the two sources, human.rs:345 walks the directory for conflict markers and raises the migrate signal, migrate.rs reads and then removes it. Every cli.rs test that exercises the previous layout creates the directory itself with create_dir_all, so none of them rides on init. human.rs's signal counts *files*, never the directory, which is why an empty one never showed up in check -- that is how this survived. An existing corpus is untouched: init only ever created a missing directory and never deleted one, so a checkout that already has .ank/log keeps it, keeps being read, and keeps being migratable.
