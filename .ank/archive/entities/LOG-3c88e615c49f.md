---
id: LOG-3c88e615c49f
type: log
title: Readings taken. plane_bytes counts the objects check's batch moves, claims and proof, each object
created: 2026-09-14T09:45:59Z
author: claude-code/opus-5+foreign-namespace
scope:
  - crates/ank-cli/src/config.rs
  - crates/ank-cli/src/human.rs
  - crates/ank-cli/src/init.rs
  - crates/ank-contract/**
  - crates/ank-cli/tests/cli.rs
  - docs/**
about: TASK-b858b5965b07
seq: 2
schema: 4
version: 1
---

 once, because the batch is narrowed to those namespaces since TASK-dd3ab6cb2dcc and counting the mirror or a foreign namespace would need their sizes, i.e. for-each-ref %(objectsize) in git.rs, outside this scope; the mirror's proofs (1.45 MB here) stop arriving with TASK-21de and foreign namespaces have their own signal. hot_files counts .md files in .ank/entities/ only, parseable or not; the previous layout has its own signal. An absent weight key or counter takes the default, as an absent context_budget does, so a corpus predating the key is still weighed. Defaults: hot_files 3000 against 1979 today (66 percent), crossed within about a month at August's 1645 entities unless ank archive moves the 1435 log entries; plane_bytes 4000000 against 3285575 today (82 percent), because that batch is almost all proofs appended once per CI run (TASK-dd3a measured ~250 ms of YAML parse for it) and the next growth of that mechanism should be a finding, while a compacted plane is a few hundred bytes per fact. weight.hot_files and weight.plane_bytes are addressable by ank config (ADR-e64d8b), written verbatim as numbers; Key::Under gained verbatim and default for that. TDD: the five binary tests went red on 'unknown field weight' before any code; the two config.rs unit tests were written after the parser and were green on first run.
