---
id: LOG-a92a9ed57947
type: log
title: "discrepancy: the criterion asks for a task reported when its version exceeds what its records"
created: 2026-08-22T20:30:56Z
author: claude-code/opus-5
scope:
  - crates/ank-cli/src/human.rs
  - crates/ank-cli/src/claim.rs
  - crates/ank-cli/src/commands.rs
  - crates/ank-cli/tests/cli.rs
about: TASK-cbc6963fd0ef
seq: 0
schema: 4
version: 1
---

 account for, naming both counts, and what landed names both hashes instead. The decision it asked me to make first is what moved it. Putting a version on the records was the shape the task was filed with, and it does not repair the arithmetic: a version in the claim record and in the completion ref accounts for the claim that is live and the done that settled it, and loses every version of every cycle in between, because the record that carried it is the ref release deletes. So the accounting stops counting for a task and compares content instead, which is ADR-f7dc76886db2, proposed and waiting for a human. Two clauses of the criterion are met whole: a signal never a fault, and not one finding on a corpus that predates the regime. The falsification is stronger than the one asked for rather than weaker -- the criterion wanted a hand edit with the version bumped, and what is tested is a hand edit that leaves the version exactly where it was, which no count can see at all. The count is kept for an adr and a spec, where it catches the one case a hash cannot: a version moved and nothing else.
