---
id: LOG-879c2ce798a1
type: log
title: peer constraints are served in orientation mode and deliberately not in execution mode. Execution
created: 2026-08-15T08:45:52Z
author: claude-code/federation-read
scope:
  - crates/ank-cli/src/config.rs
  - crates/ank-cli/src/repo.rs
  - crates/ank-cli/src/context.rs
  - crates/ank-cli/tests/cli.rs
about: TASK-13e802e46050
seq: 1
schema: 3
version: 1
---

 serves the set claim hashes into the claim record, and a rule from a corpus refs/ank cannot reach has no place in a freeze: claims do not cross, and a hash that moved when a sibling checkout changed would make done warn about something no reader here could show. Two consequences noticed while measuring: check would have faulted every cross-corpus scope entry as a dead scope, since it matches no local file by construction -- the entry is now excluded from the local file match and its glob half still validated; and the peer is opened through Store and never Index, because Index::open writes index.db into whatever corpus it is pointed at, which the integration test catches by comparing the peer's bytes.
