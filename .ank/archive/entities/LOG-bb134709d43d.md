---
id: LOG-bb134709d43d
type: log
title: Fix measured through the binary at every branch, target/debug/ank against a scratch
created: 2026-08-31T03:00:23Z
author: claude-code/opus-5+corpora
scope:
  - crates/ank-cli/src/config.rs
about: TASK-409e4c7d8aac
seq: 2
schema: 4
version: 1
---

 XDG_CONFIG_HOME. Newer schema, with and without an unknown key: 'ank status' exits 9 with "schema 2, this binary reads 1: the file is newer than this ank" and the upgrade hint -- the same sentence parse() gives for .ank/config.yml, never a field. Readable schema with an unknown key: unchanged, "unknown field `mirrors`, expected `schema` or `corpora`", so a typo stays a typo. Schema 0 keeps its own "schema 0 is not 1": below the range there is no older tool to name. Write paths: 'ank config --user corpora.<id> <path>' now exits 1 with the same sentence and leaves the file byte-identical, where before the guard it exited 0 and wrote into it; 'ank init --at' refuses through declare_corpus identically. A line-addressed read ('ank config --user corpora.<id>', no value) still answers at any version -- refusing it would leave a reader unable to see what the file says, and it parses nothing.
