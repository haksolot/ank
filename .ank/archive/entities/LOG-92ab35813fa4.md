---
id: LOG-92ab35813fa4
type: log
title: "Negative control run rather than assumed: neutralised only the probe branch in parse() (if let"
created: 2026-08-30T19:05:38Z
author: claude-code/opus-5+schema
scope:
  - crates/ank-cli/src/config.rs
  - crates/ank-cli/tests/schema.rs
  - .ank/config.yml
about: TASK-742cd978a806
seq: 5
schema: 4
version: 1
---

 Some(found) = Option::<u32>::None) and re-ran tests/schema.rs. Both newer-schema tests go red and the four non-regression tests stay green, so the suite discriminates on exactly the change. It also corrected a claim of mine: the nested case was already refused on the version before the fix, as 'unknown schema 2 (supported: 1)', because ADR-443590981e41's retyping of verifiers to a Mapping had moved that parse after the schema check. Only the top-level deserialize was field-first. The nested test therefore pins the sentence rather than the field-vs-version branch, and its doc comment now says that instead of overclaiming.
