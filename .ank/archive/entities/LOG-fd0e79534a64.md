---
id: LOG-fd0e79534a64
type: log
title: "Review before done. Against the criterion: six lines with name, description and declared revision"
created: 2026-09-13T12:58:06Z
author: claude-code/544e
scope:
  - crates/ank-cli/build.rs
  - crates/ank-cli/src/**
  - crates/ank-contract/src/verbs.rs
  - crates/ank-cli/tests/skill.rs
about: TASK-544ec9655570
seq: 4
schema: 4
version: 1
---

 (skills.rs listing, test ank_skills_prints_one_line_per_skill_it_carries); every embedded body byte for byte against the tree (install test compares each written SKILL.md to the manifest's file); --install writes a new temp directory with one subdirectory per frontmatter name then runs npx skills add <dir> (stub test, argv recorded, written:yes before npx ran); exit code is the stub's 42; no npx on PATH prints the directory and the command, exit 0 (no-node test); stdin closed on every run, the stub recording stdin:closed; help --json carries skills with --install and help.json is re-blessed; the no-skill/ build answers one line (unit test on the function plus the measured build logged above). Nothing found beyond the criterion except the --json refusal, which section 4 forced. Against the constraints (ank scope over build.rs, src, verbs.rs, tests/skill.rs, tests/cli.rs, golden-json): ADR-e1d7 holds (nothing beside the executable, no network attempted by the verb, npx failure is the verb failing with its output as the reason); ADR-6fd6 holds (help --json generated from the table, golden pinned, output declared empty); ADR-9307 holds (no git, dispatched before startup); ADR-d3a8 and no em dashes in added prose. Nothing found. Workspace suite on Windows: every binary ok, ank-cli cli.rs 333 passed, skill.rs 32 passed, 0 failed.
